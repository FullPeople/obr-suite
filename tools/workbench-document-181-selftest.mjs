// Diagnostic: actual installed SDK + actual iframe boundary; synthetic HTTP room.
// This probes remote notification races without changing production modules.
import {build} from 'rolldown';
import {readFileSync,writeFileSync,mkdirSync,unlinkSync} from 'node:fs';
import {resolve,join} from 'node:path';
import {createServer} from 'node:http';
import {createHash} from 'node:crypto';
import {createRequire} from 'node:module';
const out=process.env.WORKBENCH_DOCUMENT181_OUT||'F:/CodexWork/2026-09-20/w-xu/workbench-document-181';mkdirSync(out,{recursive:true});
const source=readFileSync(process.env.PROFILE_SOURCE||'src/workbench/background.ts','utf8');
writeFileSync(join(out,'background-baseline.ts'),source);
const expose=`Object.assign(window,{liveProbe:{catalog,snapshot,hydrate,refreshSelection,command,activate(){child=window.parent;chosen='card:hero';lastSelection='["one"]';},expire(){documentTimes.clear();},cache(){return {revision:documents.get('profile-room:card:hero')?._suiteRevision,time:documentTimes.get('profile-room:card:hero'),flight:cardReads.has('profile-room:card:hero'),chosen};}}});`;
const profiled=source.replace("setInterval(()=>send('pong',{at:Date.now()}),10000);",'').replace('setInterval(()=>{changed();scheduleInventoryRepair();},4000);',expose);
if(!profiled.includes(expose))throw Error('Probe hook no longer matches background');
const entry=resolve('tools/workbench-document-181-entry.ts');
writeFileSync(entry,`import OBR from '@owlbear-rodeo/sdk';import {setupWorkbench} from '../src/workbench/background';(window as any).wbMock={metadata:{},settings:{},emit(){}};OBR.onReady(()=>setupWorkbench());(window as any).profileModuleReady=true;`);
try{await build({input:entry,plugins:[{name:'probe-boundary',transform(code,id){if(id.replaceAll('\\','/').endsWith('/src/workbench/background.ts'))code=profiled;return code.replaceAll('import.meta.env.BASE_URL',JSON.stringify('/suite-dev/')).replaceAll('import.meta.env.DEV','false');},resolveId(id){if(id==='./state'||id==='../state'||id==='../../state'||id==='../modules/bestiary/data')return resolve('tools/fixtures/workbench-modules.ts');}}],output:{file:join(out,'probe.js'),format:'esm',codeSplitting:false}});}finally{unlinkSync(entry);}
const list=Array.from({length:3},(_,i)=>({id:i?'hero'+i:'hero',name:'卡'+i,owner_ids:['other'],visibility:'public',locked:false,...(i?{}:{url:'http://127.0.0.1:5601/characters/upload-room/hero/data.json'})}));
const runtime={stats:{health:20,'max health':30,'temporary health':0,'armor class':15},conditions:[],resources:{}};
const items=list.map((c,i)=>({id:i?'token'+i:'one',name:c.name,type:'IMAGE',layer:'CHARACTER',createdUserId:'other',position:{x:i,y:i},metadata:{'com.character-cards/boundCardId':c.id,'com.obr-suite/bubbles/data':runtime.stats,'com.obr-suite/workbench/runtime-baseline':{version:1,cardId:c.id,revision:1,value:runtime}}}));
const scene={'com.character-cards/list':list,'com.obr-suite/workbench/shared':{id:'profile',revision:0}},room={'com.obr-suite/workbench/cards':list,'com.obr-suite/workbench/owner-roles':{me:'PLAYER',other:'PLAYER'}};
const docs=Object.fromEntries(list.map(c=>[c.id,{schema_version:'0.3',_suiteRevision:1,identity:{character_name:c.name},core_stats:{hp:{current:20,max:30,temp:0},ac:15},inventory:{},features:{},background:{},classes:[],web_conditions:[]}]));
const parentScript=`window.state=${JSON.stringify({items,scene,room})};window.results=[];window.rpc=[];window.blockProjection=false;window.projectionBlocked=[];window.projectionEvents=[];window.broadcasts=[];
window.addEventListener('message',event=>{const m=event.data,writer=event.source===document.querySelector('#writer')?.contentWindow;if(m.protocol==='full-suite-workbench/v1'){results.push({at:performance.now(),writer,type:m.type,key:m.state?.key,revision:m.document?._suiteRevision,conditions:m.document?.web_conditions,health:m.document?.core_stats?.hp?.current,document:m.document});return;}if(!m.id||!m.nonce)return;rpc.push(m.id);const s=state,d=m.data;let value={};switch(m.id){
case 'OBR_PLAYER_GET_ID':value={id:writer?'writer':'me'};break;case 'OBR_PLAYER_GET_CONNECTION_ID':value={connectionId:writer?'writer-connection':'local-connection'};break;case 'OBR_PLAYER_GET_ROLE':value={role:writer?'GM':'PLAYER'};break;case 'OBR_PLAYER_GET_NAME':value={name:writer?'写入者':'只读玩家'};break;case 'OBR_PLAYER_GET_COLOR':value={color:'#50525B'};break;case 'OBR_PLAYER_GET_SELECTION':value={selection:['one']};break;case 'OBR_PLAYER_GET_METADATA':value={metadata:{}};break;case 'OBR_PARTY_GET_PLAYERS':value={players:[]};break;
case 'OBR_SCENE_IS_READY':value={ready:true};break;case 'OBR_SCENE_GET_METADATA':value={metadata:s.scene};break;case 'OBR_ROOM_GET_METADATA':value={metadata:s.room};break;case 'OBR_SCENE_ITEMS_GET_ALL_ITEMS':value={items:s.items};break;case 'OBR_SCENE_ITEMS_GET_ITEMS':value={items:s.items.filter(i=>d.ids.includes(i.id))};break;
case 'OBR_SCENE_SET_METADATA':Object.assign(s.scene,d.update);event.source.postMessage({id:'OBR_SCENE_METADATA_EVENT_CHANGE',data:{metadata:s.scene}},event.origin);break;case 'OBR_ROOM_SET_METADATA':Object.assign(s.room,d.update);event.source.postMessage({id:'OBR_ROOM_METADATA_EVENT_CHANGE',data:{metadata:s.room}},event.origin);break;
case 'OBR_SCENE_ITEMS_UPDATE_ITEMS':for(const u of d.updates)Object.assign(s.items.find(i=>i.id===u.id),u);if(blockProjection&&writer){const update=d.updates.find(u=>u.id==='one'),entry={at:performance.now(),revision:update?.metadata?.['com.obr-suite/workbench/runtime-baseline']?.revision,health:update?.metadata?.['com.obr-suite/bubbles/data']?.health,released:false};projectionEvents.push(entry);projectionBlocked.push(()=>{entry.released=true;entry.releasedAt=performance.now();event.source.postMessage({id:m.id+'_RESPONSE'+m.nonce,data:{}},event.origin);});return;}for(const f of document.querySelectorAll('iframe'))f.contentWindow.postMessage({id:'OBR_SCENE_ITEMS_EVENT_CHANGE',data:{items:s.items}},location.origin);break;
case 'OBR_BROADCAST_SEND_MESSAGE':broadcasts.push({at:performance.now(),writer,...d});for(const f of document.querySelectorAll('iframe'))f.contentWindow.postMessage({id:'OBR_BROADCAST_MESSAGE_'+d.channel,data:{connectionId:writer?'writer-connection':'local-connection',data:d.data}},location.origin);break;
}event.source.postMessage({id:m.id+'_RESPONSE'+m.nonce,data:value},event.origin);});`;
const currentRoomCopies=structuredClone(docs);const writesSeen=[];let blockedId='',release,waiting=false;const reads=[];
const server=createServer(async(req,res)=>{const u=new URL(req.url,'http://localhost');res.setHeader('Content-Type','application/json');if(u.pathname==='/'){res.setHeader('Content-Type','text/html');res.end('<script>'+parentScript+'</script><iframe src="/child?obrref='+Buffer.from('http://127.0.0.1:5601 profile-room').toString('base64')+'"></iframe>');return;}if(u.pathname==='/child'){res.setHeader('Content-Type','text/html');res.end('<script type="module" src="/probe.js"></script>');return;}if(u.pathname==='/probe.js'){res.setHeader('Content-Type','text/javascript');res.end(readFileSync(join(out,'probe.js')));return;}
if(u.pathname.startsWith('/characters/')){const room=u.pathname.split('/')[2],id=u.pathname.split('/')[3],stored=room==='upload-room'?docs:currentRoomCopies,body=JSON.stringify(stored[id]);res.setHeader('ETag','same-second-same-length');if(req.headers['if-none-match']==='same-second-same-length'){res.writeHead(304);res.end();return;}reads.push({id,revision:docs[id]?._suiteRevision,at:Date.now()});if(id===blockedId){blockedId='';waiting=true;await new Promise(r=>release=r);waiting=false;}res.end(body);return;}
if(u.pathname==='/suite-dev/relay'){if(req.method==='GET'){setTimeout(()=>{if(!res.destroyed)res.end('[]');},20000).unref();return;}let parts=[];for await(const p of req)parts.push(p);let bytes=Buffer.concat(parts);if(req.headers['content-encoding']==='gzip')bytes=(await import('node:zlib')).gunzipSync(bytes);const body=JSON.parse(bytes);if(body.saveCard){const s=body.saveCard;writesSeen.push({room:s.room,card:s.card,inventoryRoom:s.inventoryRoom,paths:s.changes.map(c=>c.path)});const doc=(s.room==='upload-room'?docs:currentRoomCopies)[s.card];if(createHash('sha256').update(JSON.stringify(doc)).digest('hex')!==s.expected){res.writeHead(409);res.end('{"error":"conflict"}');return;}for(const c of s.changes){let target=doc;for(const part of c.path.slice(0,-1))target=target[part]??=(typeof part==='number'?[]:{});const key=c.path.at(-1);if(c.remove)delete target[key];else target[key]=c.after;}}
res.end(body.sharedDocument?'{"revision":0,"data":null}':'{}');return;}res.writeHead(404);res.end('{}');});await new Promise(r=>server.listen(5601,'127.0.0.1',r));
const {chromium}=createRequire('D:/Desktop/DND-card-web/package.json')('@playwright/test'),browser=await chromium.launch({channel:'msedge',headless:true});
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const reports=[];
try{
const page=await browser.newPage();await page.goto('http://127.0.0.1:5601/');const frame=page.frames().find(f=>f.url().includes('/child'));await frame.waitForFunction(()=>window.profileModuleReady);await page.evaluate(()=>document.querySelector('iframe').contentWindow.postMessage({id:'OBR_READY',data:{ref:'test',userId:'me'}},location.origin));await frame.waitForFunction(()=>window.liveProbe);

const coins={cp:11,sp:12,ep:13,gp:14,pp:15};docs.hero.inventory={currency:{wallet:coins,total_gp:182.81,total_gp_raw:'182.81'}};
docs.hero.dnd_card_web={schemaVersion:1,id:'hero',name:'卡0',player:'玩家',revision:1,selections:[],runtime:{hp:20,tempHp:0,resources:{}},inventory:{coins},spellSettings:{mode:'prepared',prepared:[],slots:{}}};
currentRoomCopies.hero=structuredClone(docs.hero);
await frame.evaluate(()=>{window.liveProbe.activate();return window.liveProbe.refreshSelection();});await frame.waitForFunction(()=>window.liveProbe.cache().revision===1);
await page.evaluate(()=>{const f=document.createElement('iframe');f.id='writer';f.src=document.querySelector('iframe').src;document.body.append(f);});await page.waitForFunction(()=>!!document.querySelector('#writer')?.contentWindow?.profileModuleReady);const writer=page.frames().at(-1);await page.evaluate(()=>document.querySelector('#writer').contentWindow.postMessage({id:'OBR_READY',data:{ref:'test',userId:'writer'}},location.origin));await writer.waitForFunction(()=>window.liveProbe);await writer.evaluate(()=>window.liveProbe.snapshot('card:hero'));
const changes=[['name','新名字'],['class','法师'],['background','学者'],['race','精灵'],['spell','魔法飞弹']];
for(const [kind,name] of changes){
 const before=structuredClone(docs.hero),native=structuredClone(before.dnd_card_web);native.revision++;
 if(kind==='name')native.name=name;else native.selections.push({id:'choice:'+kind,entry:{id:'test:'+kind,name,english:kind,kind,source:'TEST',edition:'both',packId:'test',revision:'1',entries:[],raw:{}},level:1,quantity:1});
 const data=structuredClone(before);data.dnd_card_web=native;if(kind==='name')data.identity.character_name=name;else if(kind==='class')data.classes=[{name,level:1}];else if(kind==='background')data.background={name};else if(kind==='race')data.identity.race={name};else data.spellcasting={spells:[{name}]};
 const at=Date.now(),result=await writer.evaluate(m=>window.liveProbe.command(m),{type:'save',itemId:'one',key:'profile-room:card:hero',previous:before.dnd_card_web,native,previousData:before,data,observed:before});
 const committed=docs.hero._suiteRevision;if(committed!==before._suiteRevision+1)throw Error('Write diverged from upload-room source for '+kind+': '+JSON.stringify(writesSeen.at(-1)));
 await frame.waitForFunction(revision=>window.liveProbe.cache().revision>=revision,committed);
 const delivered=await frame.evaluate(()=>window.liveProbe.snapshot('card:hero'));
 if(JSON.stringify(delivered.document.dnd_card_web)!==JSON.stringify(result.snapshot.document.dnd_card_web))throw Error('Reader native document mismatch '+kind);
 if(JSON.stringify(delivered.document)!==JSON.stringify(docs.hero))throw Error('Reader full document mismatch '+kind);
 reports.push({name:'two-actual-sdk-hosts-save-'+kind,readerRevision:delivered.document._suiteRevision,deliveryMs:Date.now()-at,writeLocation:writesSeen.at(-1).room,originalRoomPreserved:currentRoomCopies.hero._suiteRevision===1});
}
if(currentRoomCopies.hero._suiteRevision!==1)throw Error('Current-room shadow copy was mutated');
const card=await frame.evaluate(async()=>{const value=await window.liveProbe.catalog();return value.cards.find(c=>c.id==='hero');});if(JSON.stringify(card.coins)!==JSON.stringify(coins))throw Error('Catalog coins not normalized '+JSON.stringify(card.coins));
reports.push({name:'constant-etag-does-not-hide-announced-revision',etag:'same-second-same-length',readerRevision:(await frame.evaluate(()=>window.liveProbe.cache())).revision,allChangedFieldsSeen:true});
const result={sourceSHA256:createHash('sha256').update(source).digest('hex'),actualSdk:true,actualCrossWindow:true,realRoomVerified:false,reports,reads,writesSeen};writeFileSync(join(out,'results.json'),JSON.stringify(result,null,2));console.log(JSON.stringify(result,null,2));
}finally{if(release)release();await browser.close();server.closeAllConnections();await new Promise(r=>server.close(r));}
