# Full Suite-dev 1.0.181 修错发布

2026-09-24。用户明确要求「除了 buff 其他 bug 修复并部署」。当前线上基线为 1.0.180-dev，稳定版为 1.3.5。

## 范围

发布角色跨房读写/缓存刷新、旧币种导致的 React 白屏、库存连续换格/字段覆盖/迟到回执/场景隔离/格位持久化，以及通知预热和首次音效阻塞修复；法术格高度翻倍，恢复右侧环阶。同步及通知原理和针对验证见 WORKBENCH_SYNC_181.md、WORKBENCH_NOTICES_181.md。

不发布这轮新增的 28 套 Buff 视觉、内置 LOCAL-BUFFS 目录、死亡豁免新图形和自定义效果模板选择器。180 已有的 18 种状态和自适应/A4 效果继续保留。状态身份比较等独立错误修复继续发布，避免因相同视觉误删其他来源状态。

Web 使用独立候选 `F:/CodexWork/2026-09-20/w-xu/release181-fixes/web`，与从线上获取并验过哈希的 180 源码对照。原 `D:/Desktop/DND-card-web` 保留全部尚未发布的 Buff 开发工作。构建使用 DND_CARD_WEB_ROOT 指向候选，两份公开源码包必须与此次实际候选一致。

## 发布要求

同时部署开发版中继，绑定实际库存房间与原上传文档房间，保留旧180宿主同房请求兼容。保留网页与中继回滚，expected=1.0.180-dev；稳定版完整树哈希必须不变。完成后验证服务器文件、HTTPS资源和源码包、中继通信/权限/CAS，并删除本次独立探测记录。

发布后的多人房间仍需实际联机使用验证；本地 SDK/网络夹具与发布探针不等同实房验收。

## 发布完成

2026-09-24，1.0.181-dev 已部署。本轮新增 Buff 仅保留本地，没有进入发布产物或公开源码包。

- 独立修错候选：109 项单元和 18 项浏览器回归通过，无跳过；包含当前卡远端刷新、40 次保存时序、库存连续移动/迟到确认、旧钱包白屏、法术格高度和右侧环阶。原 18 种状态保留，28 种新增视觉及模板选择器缺席。
- 完整 Web、Suite、设置、音乐、三龙牌及骰子构建通过。597 个服务器文件匹配；237 个 HTTPS 虚拟主机资源与两份源码包经服务器回环逐项核对。外部 HTTPS 确认版本 181 及 workbench 首页哈希。
- 开发版中继同步升级并重启；注册/送达/权限隔离/持久 CAS、新宿主库存房间绑定与旧库存拒绝检查通过。仅使用独立探针，两条探针文档均已删除。
- 稳定版完整 335 文件未变，仍 1.3.5。网页回滚目录 `/var/www/obr-plugins/suite-dev-before-workbench-1.0.181`，中继备份 `/opt/obr-workbench-relay-dev/before-1.0.181`。
- 正式收据：`workbench-test-output/deployment-1.0.181-dev.json`、`public-1.0.181-dev.json`、`scope-1.0.181-dev.json`；候选范围及浏览器验收保存在 `release181-fixes/scope-audit.json` 和 `WEB_RELEASE_ACCEPTANCE.md`。

未登录真实多人房间验证，不据此宣称实机延迟或全部联机操作已经验收。线上源码包保留构建时的候选记录，本节为部署后追加的正式记录。后续发布必须显式决定是否纳入主 Web 仓库中未发布的 Buff 工作。
