# Dev 1.0.149-dev 发布记录

用户要求：先修复影响运行的问题，部署一次 dev 并提供测试清单，在用户测试期间暂停新增功能和后续目标推进。

2026-09-09 11:53:47（北京时间）已部署：

- 安装地址：https://obr.dnd.center/suite-dev/manifest-dev.json
- 版本：**1.0.149-dev**。
- 运行源码：`a74f50daa156f4372553ad3df39916af7b0e6dec`。
- 在线构建文件与散列：https://obr.dnd.center/suite-dev/release.json
- 发布包 SHA-256：`94e100f9c5e97f43ded1102d5047d9a82583ae71ccd7b3b695bfa7fab5d4e249`。
- 用户测试：[逐项清单](DEV_TEST_CHECKLIST_20260909.md)。

## 本轮运行修复

修复骰盘、快捷骰盘、投骰动画／历史／回放、界面图标和设置说明图固定请求 `/suite/` 的问题。HTML 使用构建工具识别的公开资源地址；运行代码使用现有的通道地址函数。dev 的图片随 dev 加载，不再依赖稳定版的同名文件。没有加入新的产品功能。

使用干净提交快照、锁定的 95 个依赖进行隔离构建。伙伴页英文翻译的四份未完成工具／审核草稿保留在工作目录，不进入本次发布；工程英文 XLSX 不作为公开模板。原始中文模板保持原文件，角色卡服务器没有修改或重启。

## 验证范围

- 原有前端基线 **43/43 组**回归运行通过，覆盖模块生命周期、设置、HP／Boss、共享视野、转场／传送门、音乐原生音频、多实例三龙牌、角色卡读写、资源、共享指针、血条尺寸、圈图、先攻等。
- 资源路径修改后重新通过 TypeScript + dev 构建；重新运行设置内容检查，以及实际浏览器的 28 个设置页视图／12 个常用栏宽度检查。
- 构建产物 **948 处**HTML、CSS、JavaScript 本地资源引用解析成功；运行代码无固定指向 `/suite/` 的请求。
- 线上 **343/343 文件**通过 HTTPS 读取并逐字校验发布包；跨域响应头与 JS／CSS／HTML 类型正确，HTML 保持重新验证缓存策略。
- 角色卡服务健康检查返回 HTTP 200、`ok: true`；这不代表完成实际上传／绑定测试。
- 服务端逐文件核对 stable **312 个文件**部署前后完全一致；公网稳定版 manifest、背景入口及背景脚本也保持一致。
- 当前启动脚本 gzip 151.2 kB，46 个初始脚本块；总 JS gzip 502.0 kB。这是包体数据，不代表真实房间的加载秒数或帧率。

这些证据涵盖构建、浏览器界面和模拟 SDK 边界；没有将它们当作真实 Owlbear 多人、原生图库、手机、血条宿主缩放或大地图验收。以上体验交给本次用户实测。

## 部署与回退

上传包先校验散列，再解压到仅用于本次 dev 的新目录。核对每个文件后原子切换 dev 目录，服务器及 stable 不重启、不覆盖。额外保留 **91 个**旧版本的内容散列资源，供已打开的旧窗口延迟请求使用；全员刷新后使用新的入口。

旧 dev 原目录完整保存在服务器：`/var/backups/obr-suite-dev/1.0.148-dev-20260909T035347Z`。回退时先核对当前 dev 版本和备份散列，把备份复制到同一文件系统中的新暂存目录，再仅交换 dev 目录；保留当前版本以便恢复。不要执行旧部署脚本里的全目录删除流程，也不要覆盖 `/suite/`。

发布包、构建日志、原始 43 组回归日志、最终设置复核、HTTP 散列清单和部署脚本保存于 `U:\枭熊插件\_audit\2026-09-09\dev-release-1.0.149`。隔离构建环境在本机临时盘，U 盘源码仍是后续唯一迭代基线。

## 暂停交接

部署完成后停止新增功能和自动续做，不从伙伴页英文草稿继续扩展，不新建后续目标。等用户提供实测反馈后，优先修复影响跑团的问题。整体路线尚未完成，不能把这次 dev 发布标记为整体目标完成。

当前目标工具只提供“完成／阻塞”状态变更，不提供“暂停”；因此未伪造目标已完成或已阻塞。后续工作按用户暂停要求等待，系统目标状态的暂停需由应用提供的用户控制完成。
