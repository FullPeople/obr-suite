# Full Suite 1.0.191-dev

同步国内单机站已经验证的紧凑顶栏、角色列表、Logo 等比缩放和 Wiki 宽屏双栏。角色标签栏不因紧凑样式参与纵向扩张。底部保存条不占页面高度，连接/错误诊断保留。

资料来源继续按房间/场景共享，只有 DM 可写。仅全新默认规则新增 `autoSourceDefaults`，让所有资料默认可读；既有房间规则不迁移到本机来源配置，也不重置 DM 的选择。前端按已加载目录补全默认列表，DM 修改时写入明确快照。

国内单机、Pages 与新下载包同步发布 191。稳定版 1.3.5、原公告、传输中继和 nginx 不改；排除未发布的 Buff 与撤回的屏幕适配实验。完整发布回执保存在发布工作区。
