# 角色卡辅助页英文与次元袋计算

本批在第三十六批伙伴工程卡上继续，完成背景／起源、背包、范围示意、网页导入和 2014 据点页的固定文字。新增 176 条定位审核，累计 10,506 条；实际应用 198 个文字单元格、8 个输入提示、2 条下拉及 12 个公式。2024 仅额外应用默认自定义背景的 C2:H2 六项正文，其他背景资料没有批量替换。

## 计算修复

- 2024 主卡 `P62` 原引用 `背包!AV27/BG48`，这两格实际属于账本空白区。改为读取 `AS27` 的实际开关、`BD47` 的袋重和 `BD48` 的物品重量。Yes／有按袋重，No／无按物品重量；实际袋重可改，默认仍为 15。
- 2014 主卡 `AC62` 接受 Inactive 和原无效值；两个袋子的 Active／有效分支保持。空白及其他原有输入语义未另行改动。
- 2024 自定义背景的英文默认值通过背景 I2 映射回原查询键；A2 下拉显示英文，原中文输入和其他背景继续查询。保留 A2 的数组公式标记。
- 2014 据点的八个动态表头改用英文后缀，玩家输入的据点名和空白条件保持。

## 可读性

范围示意只修改字样和字体，保留格距、图形、颜色及所有尺寸。窄栏采用 Qty.、Coin、Name、Origin 等短标题。2024 起源页仅第 4、6 行增至 28 磅，并对六个查询结果启用英文换行，完整默认正文可读。

网页导入说明改为上传工作簿或复制 JSON 的实际操作。B8 保留完整公式和内容，以单行显示，避免长 JSON 向上溢出遮挡说明；单元格内仍可复制全部内容。没有宣称解决手工复制 AV1/B8 的换行／制表符兼容问题。

## 原生检查与文件保真

使用独立配置、禁用宏的 LibreOffice Calc 26.8，以未保存模板模型打开；本批没有写回原卡或保存计算缓存。

- 原卡复现：背包 AP29 填 12.5、AS29 填 4，BD48 得到 50；2024 旧主卡切换有／无时 P62 均为 0。
- 最终卡：2014 29 组、2024 25 组计算检查通过。覆盖数量乘重量、双语开关、袋重改为 7、物品数量改为 8、十进制收支、负余额和文本备注；据点八个命名／空白表头，以及自定义背景两种语言和另一背景查询。
- 原生读取全部 198 个新文字值和 10 项提示／下拉；主卡 A1:CQ100 的默认值及实际 `主要!AV1`、`网页导入!B8` 与本批之前完全一致。
- 查看九张页图。初版背景短栏及起源正文截断，网页 JSON 上溢，均已修正并复看；最初 2024 起源预览范围少了 BN:BO，改为实际 BO 边界。最终图中所有本批固定说明可读；长 JSON 作为可复制数据只显示一行。
- 独立 ZIP/XML 读取器核对 2014 的 19,416 格／68 部件和 2024 的 15,840 格／71 部件。未选择的单元格、原有样式／共享字符串、对象与其他部件保持。独立检查曾发现 A2 数组属性被通用文本替换器移除，随后改用保留属性的替换，并完成 2024 原生补验。
- 五项有效拒绝：篡改计划、输入文件、作者表数值、覆盖既有输出目录、输出写入源码目录。定位审核目录重新生成并通过 `prepare.py --verify`。
- Artifact Tool 作者表回读 920 格；未将原卡导入后重新导出。U 盘新目录重建与验证副本逐字相同。

## 最新工程卡

目录：`U:/枭熊插件/_audit/xlsx-character-pages/character_pages-candidate-20260909T223422684718Z`。

| 规则 | 文件 | SHA-256 |
| --- | --- | --- |
| 2014 | 2014-CHARACTER-PAGES-NO-CACHE-NOT-FOR-UPLOAD.xlsx | 1c341452a3d48f9616e14a7b8e02928c9a4b7837da2c119ed8dd9a76e514898c |
| 2024 | 2024-CHARACTER-PAGES-NO-CACHE-NOT-FOR-UPLOAD.xlsx | d2c97ed34fe20a45b6fa2b2c0b5d88366c4a1715d1b15db3e448576ab1db7ac1 |

前置伙伴卡哈希分别为 `59313e07baf22cdaea26d8df79a5ffddb1021de3975e2b67b23673754d11a324` 和 `58b1fb5cac29a971e4fbba4c6d8e00e35ff9fb8264a6d7a5f68800cec0c00ab6`。最终作者表哈希为 `17b098d99cb6b292056b2527aeda1983549a9e7b8f8abcd259a41e52df37dbe6`。

## 复现入口与证据

`tools/xlsx-localization/character_pages_package.py` 接收 `--input-2014`、`--input-2024`。先以 `--prepare` 输出审核计划，再用 `author_character_page_targets.mjs` 生成作者表，最后以 `--authored-targets` 生成候选；默认输出在 U 盘审计目录。`--output` 允许指定源码目录外的新 `character_pages-plan-`／`character_pages-candidate-` 目录。

`character_pages_verify.py` 独立读取 `--source`、`--output`、`--plan`、`--version`，不调用生成器检查逻辑。

本批诊断目录：`F:/CodexData/admin/.codex/tmp/obr-card-pages-20260910`。保留作者表、计划、原生探针和关闭受控进程的执行器。

- 原卡及实际错误复现：`run-20260909T2223598138641Z`。
- 最终 2014 与布局修正后两版检查：`run-20260909T2229256615978Z`。
- 保留数组标记后的最终 2024 检查：`run-20260909T2232490867727Z`。
- `character_pages-candidate-v3` 与 U 盘最新工程卡相同；早期候选及失败记录仅供诊断。

## 未完成边界

这仍是部分英文、无缓存的工程卡，不是可公开下载的全英文成品。目前还有 13,159 条无译文记录及 2,257 条未正式审核的种子，主要集中在职业、种族、专长和其他资料正文；最终 Excel／WPS、完整保存保真和真实房间导入尚待验收。本批未替换公开模板，未部署前端、服务器或稳定版，七份解析增量与旧网页模板补丁仍未安装。持续目标继续执行。
