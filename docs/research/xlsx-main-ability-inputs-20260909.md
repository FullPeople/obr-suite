# 主卡英文属性输入及服务器识别

施法属性 L24 和前三行武器属性覆盖 AA30 原先只提供中文，服务器还会把 Intelligence／Wisdom 落到默认魅力行。本批将两版默认值改为 Charisma／Default，原下拉改为六种英文属性和可选 Default，更新 D24 及前三行装备属性生产公式。英文输入先转换为原中文查找键，C13:C18 和原装备字典保持；末两行武器继续不受覆盖。原中文值保留计算含义，但不属于新的严格英文下拉列表。

实际 Calc 同时打开上一版与本版的未保存可编辑副本，每版完成 24 组检查：六种中英文施法属性、大小写、空值和未知值，以及默认和六种武器覆盖、空值和未知覆盖。测试使用不同属性修正、熟练和额外 DC，并填入五件实际武器。DC、所有受影响武器修正、末两行和完整 AV1/B8 输出与对应中文输入一致，只有导出的属性显示名称按输入改变。原有非法输入的错误语义保持。48 组全部通过，耗时 60.488 秒，无交互请求和超时，进程正常关闭。

初次实际 PDF 显示 Intelligence 和 Constitution 换行后被截断，未将其记为排版通过。随后仅给 L24／AA30 追加 Arial 8.5 样式，保留原粗体、颜色、边框和保护，关闭换行；N／AC 列宽改为 4.5。实际重新导入并导出两版 PDF，四个长输入都完整可读，耗时 28.609 秒。此轮只检查变更后的显示，不重复 48 组计算；独立读取器确认最终全部值和公式与已做计算的副本相同。

每版只改变主卡、装备 XML 和样式 XML。两种字体及样式追加，全部原样式保持。独立读取器另核对 2014 主卡 9,112 格、装备 957 格，2024 主卡 8,170 格、装备 3,026 格，原对象、其他部件和 ZIP 元数据保持。20 条目标的独立作者表实际导出并回读 84 格，SHA256 为 `6c8a4ddf5937d326abd42bb6f728c51a80136f81bfbdda6357e35ab5bb391ff6`。

| 版本 | 最终工程副本 SHA256 | 字节 |
| --- | --- | ---: |
| 2014 | `a8e797c28189ac6bd4a5c8733ff450a05479f366cd441d071288f67225559fbb` | 1,174,167 |
| 2024 | `d4676e66b358ad0c84fa28cbe3c6a7567a6c4944c377e134c49468f56f82fd60` | 1,945,804 |

`main_ability_package.py` 从明确输入重新读取原件和正式审核生成计划；22 个所需来源文件的新隔离目录重建相同字节。八次命令包含六项有效拒绝：越界输出、已有目录、优化模式、错误源版和作者表普通值／布局目标变异。这里复用已实际导出的作者表，不再重复作者或原生计算。没有新增译文审核计数。

第五份服务器补丁仅改变 `parse_spellcasting` 的属性查找键：精确配置键优先，再识别大小写不敏感的 Intelligence、Wisdom、Charisma。用户输入的显示文字保持，中文、空值、其他文字和物理属性仍走原路径。原服务器未安装此补丁；第四补丁结果 `9272d4ed…` 是其输入，结果为 `2512a21b…`。五包的新隔离目录完成精确正反向链、八项完整性拒绝和七组解析器测试，另在本批真实英文副本中分别核对三种属性对应 66／67／68 行。旧坐标检查使用真实旧配置和测试输入，不等于取得或验收原版 v1.0.0 卡。

证据位于 `U:\枭熊插件\_audit\2026-09-09\xlsx-main-ability-inputs`、`xlsx-calc-main-ability-inputs/run-20260909T0136021013109Z`、`xlsx-calc-main-ability-layout/run-20260909T0145084405078Z`、`xlsx-server-ability-inputs` 和 `phase32-validation`。Excel/WPS、原生保存缓存、完整英文、动态多行文字及上传仍待处理；没有替换公开下载、部署服务器或宣布真实房间通过。整体目标继续执行。
