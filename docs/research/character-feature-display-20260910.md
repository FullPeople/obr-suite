# 角色卡特性显示与直接阅读

悬浮卡缺少战斗风格和特殊能力，原来的名字按钮也不能阅读角色卡中已经填写的说明。现在补齐这两组，有说明的特性、专长和法术点击名称直接展开，长文字保持换行并在现有面板中滚动。需要查资料时可点搜索；没有本地说明的条目仍直接搜索。

两组新标题提供中英文。正文采用角色卡明确提供的 `description_en`/`description_zh`，否则保留作者原文，不自动篡改自定义规则。切换语言保持已展开的条目，不重新请求卡片、不写入角色或地图数据。键盘 Enter/Space 同样可操作；撤销查看权限或切换角色会移除原说明。

## 本地验证

- 定向浏览器检查先运行已提交旧代码，确认战斗风格/特殊能力均缺失且没有正文阅读区，再检查新代码。两版原生保存后上传所得 JSON 作为固定诊断样本，逐字核对特殊能力说明。
- 24 项检查涵盖五类分组、重复法术、完整中英文文本与回退、展开状态、搜索行为、标记转义、长无空格内容在 320px 面板换行、全文末尾可达、键盘操作、无额外读写与权限清理。320px 中英文实际截图已查看。
- 30 项原有 DOM 检查及 45 项整合检查通过；后者使用实际悬浮页、HP/资源编辑器和状态更新实现。Owlbear SDK、骰子及布局边界受控，不等于真实房间验收。
- 首轮新测试把所有面板同步消息都算作搜索，因此停在“打开说明不搜索”。修正为只检查 `com.obr-suite/search-query` 后通过，并按现有模拟 SDK 的 `data` 字段核对实际搜索词。初轮构建保留，不改记为通过。

工程样本为 `tools/fixtures/cc-imported-2014.json` 与 `cc-imported-2024.json`，来自[上一批原生保存与上传诊断](xlsx-dynamic-save-20260910.md)。它们只含测试填写的数据，不是完整英文卡。浏览器入口为 `tools/cc-info-entries-browser.mjs`，`CC_ENTRIES_OUT` 指定新的输出目录。

## 旧网页补丁

独立交付在 `tools/server-patches/feature-display-20260910`，只修改 Jinja 模板：增加两类缺项，完整说明默认展开，可折叠；主名称在 display_name 为空时采用 character_name。既有全屏 Suite 页面原本已显示两类，不重复改写。

| 模板 | SHA256 | 字节 |
| --- | --- | ---: |
| 本地与线上原模板 | `78910737c85ed6e22e276728a694a323af88956254c6fc03edd10aaf7a790934` | 14,123 |
| 新模板 | `dd7fcbeaf38fe8adb0232f15dad6901168517c6023f023d2260c6d0c16132c5b` | 14,922 |

真实渲染器运行两版原生输入、自定义双类型和没有新字段的旧 JSON，共六种输出。旧 JSON 带明确别名的结果与原模板逐字相同。实际浏览器另完成 20 项名称、正文可见性、原文逐字保留、标记转义及键盘展开收起检查。两版预览已查看。所有渲染代码、CSS、提示脚本及源模板保持原字节，准备程序拒绝覆盖已有输出。

该模板尚未安装到服务器，与七份解析补丁独立。将来部署模板后，原有静态卡页面仍需重新渲染才更新；完整英文模板和旧网页全页面英文也尚未完成。

## 发布与后续

Suite **1.0.154-dev** 已于 2026-09-10 05:55:59（北京时间）部署到 [dev 安装地址](https://obr.dnd.center/suite-dev/manifest-dev.json)，产品源码为 `6a92d9206738f37bc6ca99fab7b4c231465f3fc3`。从该提交归档到新目录，重新安装锁定依赖后完成 TypeScript 和 Vite 构建。336/336 个公网文件的内容散列、类型、缓存和跨域响应核验通过，925 个内部素材/脚本引用有效。

稳定版 1.3.0 的 432 个文件、三龙牌 0.3.2-dev 的 194 个文件和音乐台 8 个文件与部署前完整目录一致；额外核对五个公网入口保持原内容。角色卡服务器仍运行原解析器和原模板，本轮只读复核其散列及 active 状态。旧模板补丁的指纹索引已统一为 Git 保存的 LF 字节，模板与运行时代码本身不变。

| 发布证据 | SHA256 / 位置 |
| --- | --- |
| 源码归档 | `414ed61359fc9e14e71d220009e5daa190e2dab449c490898a0515cbc7a23cb6` |
| suite-dev-1.0.154-dev.tar.gz，9,423,841 字节 | `b5d277569f398087d1bef8b8291e622aa0305d13ebeddc7160632c2b719edaf1` |
| 完整私有备份 | `/var/backups/obr-feature-display-20260910/suite-dev-20260909T215559Z` |

部署采用目录原子交换，254 个旧哈希资源保留，既有打开页面所需的资源继续可用。实际回退目录、前后清单和构建日志见本轮 `release/` 证据；没有覆盖旧备份。

真人验证：在 dev 刷新后，选中带特殊能力/战斗风格的角色；确认悬浮卡出现两类、说明全文可读、语言切换保留展开，HP/资源输入继续可用，再切换到其他角色确认内容正确。完整英文下载和持续路线图仍继续推进。

完整本机证据位于 `F:/CodexData/admin/.codex/tmp/obr-feature-display-20260910`，小型证据索引保留在 `U:/枭熊插件/_audit/2026-09-10/character-feature-display`。本批没有新增 XLSX 译文审核条目，最新工程卡仍为第三十六批伙伴页卡。
