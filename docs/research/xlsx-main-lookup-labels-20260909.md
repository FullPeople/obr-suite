# 主卡属性与技能名称迁移

两版 C13:C18 的六项属性、2014 的 18 项技能使用完整英文名，沿用 reviewed.jsonl 中按原卡版本、位置和语义核对的 30 条正式译文。特技、驯养、医药、生存分别按其技能行译为 Acrobatics、Animal Handling、Medicine、Survival，不作脱离上下文的全局替换。累计正式审核仍为 10,146 条，本批不重复增加计数；这不表示整卡已经翻译完成。

这些名称原本也充当查找键。2014 的五个装备属性修正、十个旧攻击／伤害文字公式和十八个技能查找，以及 2024 的 DC 和五个装备修正，共 39 个消费者改用原名到英文名的有界对应表。对应表放在已有隐藏 Export 页的空 K:L 区域，2014 为 K1:L25，2024 为 K1:L7，包含表头；没有新增页。IFNA 仅在名称未匹配时回到输入原值，保留既有错误传播。2014 旧文字公式分别读取第 7／16 列的行为保持，没有顺带改变可疑的原列选择。

完整名称采用 Arial 8.5，每版仅追加一种字体和样式；原字体、样式、颜色、边框、保护和其他单元格保持。E 列宽为 2014 的 8、2024 的 4.5。实际四页前后 PDF 中，两版全部属性和 2014 所有技能都完整可读，包含 Sleight of Hand、Animal Handling 等长名称。该检查限于已改区域的 Calc 排版，不是全卡或 Excel/WPS 验收。

| 版本 | 最终工程副本 SHA256 | 字节 |
| --- | --- | ---: |
| 2014 | `3018996300dd3b701c1c4bdd9c1e8a44e358d9291d40940b400a0981b1eba697` | 1,175,358 |
| 2024 | `2ee05231df8435e67bc5715d66d1f621935e2db5d38e47ccf6d0c89ec2307d2b` | 1,946,060 |

实际 Calc 首轮完成 2014 的 24 组属性／武器检查，随后在旧卡导出报错处停止，未记为整轮通过。续测先在前后两卡重现相同原因：上一项故意填入的 Unknown ability 仍在 AA30，AV1 和 B8 均为 #N/A；恢复 Default 后两卡都正常。保留失败记录并从未完成的技能与旧文字公式继续，没有重跑已完成的 24 组。

最终覆盖 2014 的 30 组、2024 的 24 组：六种中英文属性、大小写、默认、无效输入、五行武器和末两行例外；2014 另验证全部十八项技能的中文／英文输入、无效技能及旧文字公式的正负值分支。有效场景的完整 AV1/B8 内容与旧卡对应值一致。首轮耗时 43.316 秒并正常结束失败诊断，续测 52.508 秒完成，无超时，Office 正常关闭；所有工作簿均未保存。

单独作者表回读 544 格，SHA256 为 `eddc5adadc3d2d2d18a99e14877af7021e9eb27372adb633dff3361ca71a6161`。独立 ZIP/XML 检查覆盖全部受影响页，确认既有 Export 单元格、全部原样式、对象、无关部件和元数据保持。新增仅为已列出的 50／14 个隐藏表格值，2014 主卡改 24 格、装备 15 格、数据表 18 格；2024 主卡改 7 格、装备 5 格。

24 个必要来源文件的新隔离目录重建相同字节，八次命令含六项有效拒绝，涵盖错源、改动作者表和输出边界。初次隔离检查遗漏 glossary.json，补齐依赖清单后使用新目录完成，旧失败日志保持。最终整合又确认本批拟新增的 30 条全部与原 reviewed.jsonl 对应项相同，删除仓库中重复草稿并保留审计副本，生成入口改为复用原审核；新隔离目录重新核对计划和同样输出，不重跑字节未变的原生卡。第五补丁结果解析器另读取本批两版真实副本，在默认值和指定属性／技能值两种场景中保持完整解析结果，共四项对照通过。没有新增或安装服务器补丁。

证据在 `U:\枭熊插件\_audit\2026-09-09\xlsx-main-lookup-labels`、`xlsx-calc-main-lookup-labels/run-20260909T0209397129580Z`、`xlsx-calc-main-lookup-resume/run-20260909T0211437848505Z` 和 `phase33-validation`。仍需完成主卡剩余默认文字、资料正文、动态多行文字、保存缓存、下载替换与实际上传；本批未部署前端或服务器，整体目标继续执行。
