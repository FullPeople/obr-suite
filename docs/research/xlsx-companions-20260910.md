# 伙伴页英文与豁免修复

两版伙伴页完成 123 个固定标题、每版四条下拉规则及 71 个完整标题提示。129 条原始定位译文已逐项核对，正式累计审核 10,330 条。2014 使用 Race，2024 使用 Species；原版魔宠技能栏与新版易伤栏保持各自含义。没有替换玩家名字、故事、属性值或 X/O 勾选。

下拉保留顺序和数量：17 项职业（包含三个协力者职业）、14 种生物类型、10 种阵营以及 6 种体型。前三项使用完整英文列表，体型复用此前已审核的隐藏 Export 对应表；没有新建玩家需要维护的配置页。既有空值和错误提示策略保持。

## 修复与排版

2014 的 `盟友与魔宠!AM3`、`AM5:AM8` 在熟练分支含 `#REF!`，勾选 O 后会计算错误。它们现在引用同页 `AB3` 熟练加值，与本版完整的敏捷豁免 `AM4` 和 2024 同位置公式一致。属性修正、额外豁免修正、空勾选及魔宠公式均保持。

英文采用 8.5 点 Arial 和通用短标题，选中对应标题可查看完整名称。第一版实际预览暴露 Resist./Immune 等窄列裁切以及 Chaotic Neutral 换行显示不全，只加宽 E、U、AX、BF 四列。最终两版 PDF 已查看，输入值和标题完整可见。行高、合并、对象、保护和原样式不变。

## 验证与边界

- 实际 Calc 26.8.0.3 验证全部标题和完整提示，以及两版所有英文下拉选项的输入保留与原有验证策略。每版七个实际下拉位置，逐一检查共 154 次选项输入；这不等于鼠标逐项点击验证。
- 十组盟友豁免场景，覆盖六项属性、熟练 0/2/4/6、全选/不选/混合/空值与额外修正。五个原缺陷仅在勾选时复现，修复结果与明确数值计算一致。
- 八组魔宠 CR/豁免场景验证 CR 0/5/13/30、对应熟练加值及混合勾选。每场景六项豁免保持原结果。
- 主卡完整 AV1/B8 导出前后相同；实际打开和计算没有保存任何 XLSX。首轮计算共 79.37 秒，最终版式检查 32.27 秒，两次均正常关闭 Office，无超时。
- 独立 ZIP/XML 读取核对全部工作表和部件；只有伙伴页和样式部分变化。原样式保留，新增字体/样式只应用于选中标题。非标题单元格、共享公式、输入值、外部关系、条件规则和导出内容保持；四列以外的宽度也逐项一致。
- 翻译清单重新生成，17 项原始内容和定位检查通过。仍有 13,245 条无英文候选内容，其中长正文 2,613 条；数量不代替已应用范围。
- 58 文件的隔离源码副本重建两版得到完全相同字节。实际拒绝了版本错配、旧版作者表、范围外输出，以及已存在的候选目录；已有文件未被覆盖。

这批为完整英文角色卡的一个组成部分，尚不是可上传的最终下载。其他资料正文、动态多行文本兼容、原生保存缓存、Excel/WPS 和真实服务导入继续待验。服务器及公开模板未改，也没有新部署。

## 工程文件与可复现入口

| 版本 | SHA256 | 字节 |
| --- | --- | ---: |
| 2014 | `59313e07baf22cdaea26d8df79a5ffddb1021de3975e2b67b23673754d11a324` | 1,176,768 |
| 2024 | `58b1fb5cac29a971e4fbba4c6d8e00e35ff9fb8264a6d7a5f68800cec0c00ab6` | 1,947,627 |

最新两份工程卡位于 `U:/枭熊插件/_audit/xlsx-companions/companion-candidate-20260909T210409240604Z`。输入为第三十五批数字格式卡，指纹由生成器固定核对；英文内容通过 Artifact Tool 作者表回读全部 840 格后才应用。最终作者表 SHA256 为 `33fe9a7e4b8ea844daf306583066d113f0989a0f19171ce522816cb66cae99cd`。

生成与独立检查命令见 `tools/xlsx-localization/README.md`。本机验证材料在 `F:/CodexData/admin/.codex/tmp/obr-companions-20260910`，含初版与最终版预览、两次原生报告、独立包检查及隔离源码重建。小型证据索引另存 `U:/枭熊插件/_audit/2026-09-10/xlsx-companions`。

后续从这两份最新卡继续资料页和导出兼容，不再回退到数字格式卡作为最新输出；后者仍是该增量生成器所需的固定输入。
