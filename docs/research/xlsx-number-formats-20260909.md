# 主卡与伙伴速度的英文单位

两版唯一含中文的自定义数字格式 `0"尺"` 改为 `0" ft."`。该格式用于主卡黑暗视觉和速度、盟友与魔宠页两个速度输入；每版四个合并输入覆盖 20 个显式单元格。已核对全部样式使用者，无其他页、条件格式或行／列继承使用者。两个完整原格式按原卡位置审核，累计 10,201 条。

只替换 styles.xml 中 numFmtId 176 的 formatCode 属性，工作表、数值、公式、已有样式 ID、输入保护、下拉、对象和几何尺寸完全不动。保留整数显示与原舍入行为，没有把数字改成文本，也没有进行单位换算。字面量后缀增加空格并采用 ft.。

实际 Calc 26.8.0.3 在两版卡各检查九种输入：空、0、5、30、60、120、-5、30.5 和自定义移动文字。每种同时核对四个输入，共 72 个显示样本；原值与类型、保护、整数显示及文字输入行为保持，仅预期后缀改变。每个场景的完整 AV1 与 B8 导出内容都与前版相同且无错误。耗时 60.654 秒，无超时，Office 正常关闭，没有保存 XLSX。四页 PDF 已查看，八个实际输入分别显示完整 120 ft.、30 ft.、60 ft.、30 ft.。伙伴页其他中文标题仍待翻译。

| 版本 | 工程副本 SHA256 | 字节 |
| --- | --- | ---: |
| 2014 | `4924af698212141d7053cc88db53b522c576ef3ab91be190780aca0a0666595c` | 1,176,084 |
| 2024 | `0bd14c6986d1163df3ca305fd2f9e4af8f352c60db0458de0f140aa336285de6` | 1,946,899 |

独立 ZIP/XML 检查确认仅该属性改变，所有工作表字节和 ZIP 元数据保持。作者表回读 12 格，SHA256 `f22272e27deac3d47933488610c95acb6d0fbbd5e023c5e3f4485858a66ca7ba`。24 文件隔离重建得到相同字节，八次命令含六项错源、作者表及输出边界拒绝。未改服务器代码，也未重复与格式无关的解析器或前端测试。

证据位于 `U:\枭熊插件\_audit\2026-09-09\xlsx-number-formats`、`phase35-validation` 和 `xlsx-calc-number-formats/run-20260909T0323154686581Z`。隔离重建使用 `F:\CodexData\admin\.codex\tmp\obr-number-format-portable`，主项目及最新卡仍在 U 盘。为保留 U 盘迭代空间，前批三个仅用于验证的复制目录已逐文件哈希核对后归档到 F 盘，搬移记录在 `phase33-validation/portable-relocation.json`；没有删除旧项目。

这些仍是未保存缓存的工程卡，不是可上传的完整英文下载。其他资料正文、动态多行文字和真实应用验收继续，整体目标未结束。
