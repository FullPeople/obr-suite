# 2026-09-08 实施进度

持续目标以 [产品路线](PRODUCT_REVIEW_20260908.md) 为范围，唯一迭代目录为 `U:\枭熊插件\obr-suite`。线上版本和源码依据见 [版本核对](DEPLOYMENT_BASELINE_20260908.md)。本文件记录实际实现与验证；研究结论、自动测试和真人房间验收分别记录。

## 第一批：加载、设置与血条稳定性

### 模块开关与场景设置

- 保留原有注册顺序，用单一执行队列追踪最后一次开关意图。启动中关闭、关闭中开启都会在当前操作结束后处理，不再跳过；同一模块不会并发注册两次。
- 启动抛错后先清理可能已注册的监听器，再做最多两次自动重试。清理失败时，清理成功前不重新启动。次数耗尽保留失败状态，设置可以重试本客户端，房间启用权限不变。
- 启动/停止超过五秒显示等待状态。没有取消协议的未结束操作不会被假装超时取消；否则旧操作可能在稍后重新注册监听器。单个 hook 长期不结束仍会阻塞后续注册，需要继续在具体模块内解决；模块内部自行吞掉的错误也不能由外层检测出来。
- 保留各模块跨场景监听器。场景未就绪或正在读取新设置时暂停新增注册工作，主操作栏完成打开后才恢复原有注册顺序。设置读取和保存回执加代次检查，旧场景请求不能覆盖新状态、废弃新读取或追加房间副本写入。
- 当前场景设置读取失败时保留缓存，不把默认开关误当成新场景设置。按 1 秒、4 秒最多再读两次，仍失败时明确提示；后续有效设置更新或重新打开场景可以恢复，不持续轮询。
- 设置中的运行状态通过本客户端消息传递，不增加持久房间字段。

已完成的针对性验证：

| 检查 | 结果 |
| --- | --- |
| `node tools/runtime-selftest.mjs tools/module-lifecycle-selftest.entry.ts` | 13 项通过：开关竞态、顺序、部分失败清理、有界重试、重入、场景暂停、慢操作提示及完成回执 |
| `node tools/runtime-selftest.mjs tools/state-freshness-selftest.entry.ts --sdk-mock` | 8 项通过：旧读取/写入、切场景失效、房间镜像、新场景读取不被废弃、读取失败不冒充就绪 |
| `node tools/runtime-selftest.mjs tools/scene-lifecycle-selftest.entry.ts` | 7 项通过：主操作栏顺序、旧刷新、快速切场景、有界失败重试与恢复 |
| `node tools/runtime-mutation-selftest.mjs` | 12 个故意破坏版本均被拒绝；只修改测试构建内存中的源码，没有修改工作区产品文件 |

### 加载、编辑与血条

- 图鉴同一 iframe 共用 6 个请求名额，每来源 2 个文件并发。单请求实际开始后 10 秒超时；来源连续 30 秒没有有效进展才停止。持续正常下载的大库不受固定总时限截断。失败来源保留其他成功结果，可原地重试。
- 图鉴渐进阶段可以筛选浏览，完整继承解析完成后才允许生成。缺少父模板的 `_copy` 条目不会变成缺失属性的单位；排序、旧缓存及 200 条搜索上限保留。重试会重新加载所选资料库，并非仅补失败文件。
- 搜索详情先显示标题和加载占位，辅助书名请求不阻塞详情；旧预览和被废弃的内容读取不能覆盖当前目标，短暂请求失败不永久缓存成缺失。
- 设置对比当前页渲染结果，无关更新不重建正文；相关变化保留稳定字段的草稿、光标、选区、滚动与说明展开状态。同一字段远端变化显示当前已保存值，保存失败可以重试。撤销 GM 权限立即生效，输入法组合输入完成后再刷新正文。草稿只存在当前打开的页面，不跨关窗/换页或字段删除持久化。
- HP 在同一 iframe 中接收带版本的目标消息，启动时握手补发；只处理当前目标相关字段。快速切换或切场景后，旧读取、失效草稿、延迟写入不能覆盖新目标。保留卡片/图鉴绑定排除、显式关闭、固定面板与旧 bubbles 数据合并规则；固定不会继续保留已经删除、隐藏或撤销编辑权限的目标。

分项验证：内容加载 15 项通过、10 个破坏变体被检测，其中直接经过真实加载器的虚拟慢库持续超过 60 秒仍完整返回 16 份文件；设置使用真实 Edge DOM 的 11 项检查通过，另有 3 个破坏变体被检测，包含实际设置样式下的窄面板检查；HP 使用实际模块和可延迟的 SDK 替身验证快速选择、旧读取/写入、初始化握手、100 次无关更新和旧数据合并，5 个破坏变体均被检测。

上述测试均由主代理在固定 Git 树 `5d89ff7cd95d7b3a39623058980719a35441ac1d` 导出的隔离目录再次运行通过；该快照不包含并行开发的 Boss/转场文件。固定快照的 stable、dev 两种构建均通过，已有迷雾算法自检仍为 **52/52**。测试后只补充本进度文档，不改变已验证的第一批产品文件。

稳定构建的启动路径 gzip 估计为 **117.1 kB / 35 chunks**，比研究时的 112.4 kB / 33 chunks 增加约 4.7 kB；全部 JS gzip 406.3 kB。本批增加了运行状态、错误恢复和请求保护，不能称作启动下载体积下降。源码已提供更早的加载反馈，但这些结果不是实际 Owlbear 房间速度、原生弹窗生命周期或多端同步验收。

复核加载测试：设置 `DYNFOG_ENTRY=tools/content-loading-selftest.entry.ts` 后运行 `node tools/dynfog-selftest.mjs`，再清除此环境变量；变异检查运行 `node tools/content-loading-mutation-selftest.mjs`。HP 检查运行 `node src/modules/hpBar/selftest.mjs`。设置 DOM 检查运行 `node tools/settings-content-selftest.mjs --mutations`；测试使用外部 Playwright 路径 `PLAYWRIGHT_PATH`，不增加产品依赖。

## 第二批：共享视野、Boss 血条与转场

已接入现有套件开关、设置与页面构建入口，不需要玩家再安装一份扩展。新增的 Boss/转场模块默认启用；共享视野默认关闭，旧房间或格式不正确的共享值不会被解释为已授权。

- 动态迷雾设置在稳定与测试频道都可见，常用界面只先呈现「玩家共享视野」和「填充地图迷雾」。照明参数、墙壁与门窗帮助折叠。GM 的基础光源右键入口也对两个频道开放；额外门窗绘制仍保留原频道范围。
- 共享源按明确光源归属、绑定角色卡归属和普通玩家创建的角色识别。私密角色不会自动加入队伍，隐藏单位和 GM/NPC 不因开启共享而获得玩家揭雾权。玩家可随时接收 DM 修改后的房间设置，不需要重装或重开设置。
- Boss 血条读取现有生命值字段；右键设为 Boss 后成为公开战斗提示，默认只呈现比例，具体数值由 DM 选择。最多同时三条，可设阶段文字与分段。个人关闭和减少动画在设置中调整，不修改 Boss 的生命值或其他玩家的显示偏好。
- Boss 使用紧凑的顶部浮层，最长 600×144 像素。其浮层矩形仍会拦截地图输入，透明背景不等于穿透。迷雾遮住 Boss token 不会自动撤销公开提示；DM 隐藏 token 或关闭其 Boss 展示后移除。
- 转场可从常用栏或设置打开，短休、长休和自定义文字在同一面板呈现。DM 可选择所有或指定玩家，玩家可本机预览；特效不更改角色的生命值、法术位或休息资源。
- 每扇传送门可独立选择沿用本机默认、无特效、眨眼或淡入淡出，已有传送门沿用原行为。新独立特效使用不接收点击的本地原生场景效果，不占用全屏弹窗。文字提示使用短时紧凑浮层。
- 休息提示的结束和关闭只清理自身特效，不能误停稍后发起的传送门动画。事件校验实际连接身份与当前 GM 权限；延迟角色读取不能覆盖已经到达的降权通知。旧场景、过期与重复演出消息不重播。
- 常用栏在窄窗口中可横向滚动，滚轮和键盘仍能到达右侧按钮；宽窗口仍按实际内容调整宽度。主界面限制浮层宽度不超过可用视口，避免英文标签把设置按钮挤出窗口。

主代理从固定 Git 树 `299774e1959d8ed7b78181c95868ca3427530c8c` 导出隔离目录，重新安装锁定依赖并验证。复核修正两处测试自身的问题：Boss 变异匹配兼容 LF/CRLF，短时转场在 SDK 微任务完成后检查元素，避免等到动画中点时因机器负载已触发清理。最终测试树为 `2bcb7e0bb88c128917d421e9f7746cf046cb5b98`；两树只差这两份测试文件，产品源码完全相同。stable 构建在前者、dev 构建在后者均通过。最后只补充本进度文档。

| 验证 | 结果 |
| --- | --- |
| `node tools/vision-selftest.mjs --mutations` | 64 条断言通过，11 个破坏变体被拒绝，覆盖实际 stable/dev setup 与 SDK builder |
| `node tools/vision-edit-dom-selftest.mjs` | 实际 Edge 中 18 条断言通过，覆盖中英、窄窗、保存失败重试、竞争选择、降权及切场景 |
| `node src/modules/bossBar/selftest.mjs` | 控制器、实际浏览器布局与配置竞态通过，4 个破坏变体被拒绝 |
| `node tools/transitions-selftest.mjs` 与 `--mutations` | 19 项回归通过，10 个破坏变体被拒绝 |
| `node tools/transitions-ui-selftest.mjs` | 实际浏览器中 9 项通过，包括双语传送门编辑与设置保留 |
| `node tools/suite-wiring-ui-review.mjs` | 12 个设置页视图、12 个工具栏宽度组合、左右拖动柄、滚动后命中按钮与角色权限检查通过 |
| 状态刷新自检、原有迷雾几何自检 | 9 项状态回归通过，原几何仍为 52/52 |

本批合计检测出 25 个故意破坏变体。日志保留在本机 `_audit/2026-09-08/phase2-validation`。稳定构建启动路径 gzip 为 **126.4 kB / 39 chunks**，全部 JS gzip 423.0 kB；相比第一批增加约 9.3 kB，不能称作下载体积优化，也不是实际房间性能测量。

真实 Owlbear 原生效果、GM 与多个玩家同步、关闭/重开、角色和场景切换、网络断开及大地图表现仍需房间验收。光源约束可以在 SDK 调用前取消旧批次，已经交给宿主的写入无法撤回；持续宿主写入失败的恢复仍是已有 Patcher 的限制，详见 `src/modules/fullFog/dynfog/VISION_POLICY.md`。源码更新不代表线上部署。

## 第三批：音乐板恢复与搜索英文资料

音乐板重新接入现有模块生命周期、所有角色的常用栏和设置，不需要另装扩展。新设置默认启用；已明确保存关闭的房间保留原选择，DM 可在音乐板设置重新打开。两个 manifest 按 [Owlbear 文档](https://docs.owlbear.rodeo/extensions/reference/manifest/) 声明 autoplay，用于后台播放；每个人初次仍需点击「启用声音」。

- 音频和 Studio 配对连接由后台持有，控制面板关闭、缩小、重开和重定位不销毁播放器。普通场景切换时，模块持续启用则保持房间播放；明确关闭模块只停止本机音频，不清空房间曲库、队列或播放进度。
- 默认全员可选曲、暂停及管理队列，DM 可改成仅 DM 操作。每个人的音量和静音仅保存在本机。共享状态使用房间元数据，按在线 GM 优先选出一个命令处理者并串行处理实际连接身份的操作，检查角色、重复命令和旧回执。
- 后台从媒体元信息读取时长，按共享播放时间安排续曲，主控无需启用自己的声音。没有逐帧网络写入；时长未知、媒体不可用或浏览器暂停后台计时器时仍需手动下一首。
- 保留原 Studio 配对码、分享码和默认曲库入口。原 Studio 的本地文件转换/裁剪继续独立可用；本地文件没有上传服务，需要先提供玩家可访问的音频网址。实读当前默认曲库为 154 条，与新面板读取结构相符。
- 房间共享曲库和队列各最多 32 项，并检查整个房间元数据容量；容量不足明确拒绝，保留原队列及其他扩展内容。旧场景音乐数据只读迁移为暂停状态，不重播旧音效。旧版 Studio 重连快照缺少版本，先恢复连接，再由使用者明确采用网页播放，避免覆盖房间的新操作。
- 搜索以玩家本机界面语言优先选取已启用资料源；英文正文缺失时依次回退并标明原文语言。禁用的库或来源不会被详情请求重新启用，已匹配本地自制内容优先，其他同来源的本地条目不再遮住远程结果。
- 索引、详情和书名缓存均包含语言及完整资料源身份。切语言、改 URL/索引路径/黑名单后取消旧代次读取；相同英文名称的 2014/2024 条目仍严格区分来源。英文搜索分类、法术时间/范围/持续时间、怪物/物品等呈现使用英文标签；中文别名继续可搜索，但不额外显示在英文结果行。
- 资料库设置新增语言选择，旧已知中文/英文镜像会推断语言，自定义库默认混合/未指定。保存失败回到已保存选项并能原地重试。英文预设使用经 jsDelivr 提供的第三方 5etools 镜像，删除旧文案中未经支持的「官方镜像」及把全部英文内容归为代码许可证的表述。

主代理先在固定树 `0cd0233a03e85d18211923a20b31d403c81f5ecc` 的隔离导出中独立验证，再修正状态测试对内置库排列的错误假设，并移除英文结果行的中文副标题。最终产品/测试树为 `7a3dfc2e6ded1e0d44b0270a73f98c866ee1502a`；受改动影响的状态、搜索和两频道构建已重新验证。最初构建命令传参错误寻找 `stable/index.html`，改用项目实际 `SUITE_BASE`/`SUITE_CHANNEL` 后构建通过。没有修改锁定依赖。

| 验证 | 结果 |
| --- | --- |
| stable + dev 构建、TypeScript | 均通过 |
| `node src/modules/musicBoard/selftest.mjs` | 实际多客户端 RoomMusic 延迟/并发/身份/角色/移交/配额/旧数据、Audio 去重淡出、Peer 旧回调隔离通过；3 个破坏变体被拒绝 |
| `node src/modules/musicBoard/autoplay-selftest.mjs` | Chrome 原生音频：未点击时 blocked 且进度为 0，控制页点击后隐藏后台播放，移除控制页后继续 |
| `node src/modules/musicBoard/browser-selftest.mjs` | 实际后台模块 + HTML/CSS + 原生 Audio；未启用声音时自动续曲只写 2 次，关闭前后进度 1.080→1.560 秒，缩小/重开未新建当前音频，个人音量不写房间，双语通过，页面错误为 0 |
| `node tools/search-locale-selftest.mjs --mutations` | 实际加载器、呈现器和 Chrome DOM 20 项通过，5 个破坏变体被拒绝 |
| 状态刷新自检 | 11 项通过，含语言单独变化通知、同值不重载、严格语言值与音乐启用迁移 |
| `node tools/suite-wiring-ui-review.mjs` | 20 个设置页视图、12 个常用栏宽度组合、双角色音乐入口和模块开关、语言保存失败重试、窄栏滚动与按钮命中通过 |

日志及界面图保留于本机 `_audit/2026-09-08/phase3-validation`。稳定构建启动 gzip **135.9 kB / 40 chunks**，全部 JS gzip 433.4 kB，较第二批启动增加约 9.5 kB；本批恢复了后台音频能力，不声称下载体积缩减或实际房间速度提升。

音乐验证使用替换 SDK 边界的实际 Chromium iframe，尚不是实际 Owlbear 多人房间、公网 Studio/PeerJS、移动浏览器或听感验收。选举和串行写入不是跨网络分区的服务端共识；失效主控、元数据并发写入、浏览器后台冻结仍需实房间确认，详见 `src/modules/musicBoard/README.md`。搜索的英文化也不等于全套本地化完成：图鉴绑定详情、角色卡前后端、其余界面和真正英文 xlsx 模板仍在后续范围。

## 第四批：图鉴英文正文与继承资料核对

图鉴目录和已绑定怪物详情按玩家语言选择已启用资料，保留中文搜索别名、每库黑名单、严格版本来源以及本地自制内容优先。同一物理资料文件只下载一次，独立配置仍分别过滤；语言与资料配置变化会隔离旧缓存、取消旧读取。

已有怪物快照只在能够确认没有手工修改时切换显示资料。新快照记录来源与内容指纹；旧快照先与当前可用来源比对。无法确认或已修改的内容保留原文并说明原因，不写回怪物、生命值或资源。绑定详情在快速选择另一个 token、切换语言或关闭场景时拒绝旧读取回填。

继承资料优先使用同库父条目，兼容语言的其他已启用库可补充缺失依赖。主代理复核补充修正：混合/未指定语言的父条目可能具有相同英文名字却包含另一语言的能力标题，导致按名替换被静默忽略。目录与详情现在均核对修改目标，再选匹配父条目；无匹配时不把未正确继承的条目当作完整怪物提供。

主代理从固定树 `8e21d61bfc10a1a28b2d7925186265b85cb1c8be` 导出隔离目录并重新安装锁定依赖，独立验证：

| 验证 | 结果 |
| --- | --- |
| stable / dev 构建、TypeScript | 均通过 |
| `node tools/bestiary-locale-selftest.mjs --mutations` | 实际 Edge 目录、详情与 DOM 28 项通过；10 个破坏变体被断言拒绝 |
| `node tools/bestiary-locale-loading-selftest.mjs` | 第一批原有加载基线仍为 15/15 |
| `node tools/content-loading-mutation-selftest.mjs` | 原有加载行为的 10 个破坏变体仍被拒绝；更新缺父条目变体的定位以匹配新的解析实现 |
| 英文怪物详情视觉检查 | 主代理检查真实浏览器截图，英文正文、标签、当前 HP 与资源页入口可见 |

稳定构建启动 gzip **136.0 kB / 40 chunks**，全部 JS gzip **433.7 kB**。证据保留于本机 `_audit/2026-09-08/phase4-validation`。未进行真实 Owlbear 房间联机验收；陌生旧快照保留原文不等于全部已翻译。角色卡、共享资源编辑组件及其余界面继续分别处理。

## 第五批：内置多人三龙牌

已接入常用栏、设置开关与 stable/dev 构建入口，玩家无需另装插件。采用 Three-Dragon Ante Legendary Edition 基础盒的默认独立玩法，2–6 人、100 张基础盒牌中每局使用 70 张普通龙牌和随机 10 张特殊牌；未加入扩展和可选 D&D 角色能力圆片。规则及简短双语说明独立实现，未复制官方卡面、美术或整段规则书。

- 任意玩家可创建牌桌，其他玩家加入或旁观，主持人开始发牌。界面仅提供当前合法动作、选择数量与必要的能力提示。弃牌堆和玩法说明默认折叠；准备新局需要确认，清除旧局并回大厅，允许换人后重新发牌。
- 主持后台持有权威牌局，逐项验证实际连接对应的席位、动作版本、合法性及重复请求。先保存再确认；存储失败不出牌，回执或房间更新失败重试原结果，不重复扣钱、出牌或发牌。
- 房间元数据仅放座位及主持摘要，牌库与所有手牌不放在共享元数据。各人视图通过原生 Web Crypto 的 ECDH/HKDF/AES-GCM 单独加密；旁观者只取得公开内容。密钥握手绑定房间、牌桌、连接和新会话，旧握手、旧密文或伪造席位不能覆盖当前会话。
- IndexedDB 保存主持浏览器中的完整私有状态，并在同一事务内检查旧版本，防止同浏览器两个旧写入相互覆盖。主持人离线暂停；原浏览器回来可恢复。无存档设备不会伪造旧牌局；原主持账号可在旧连接离线后明确确认回大厅。
- 控制器与规则按需加载，关窗保留座位和后台牌局，重开回放当前状态。普通场景切换不自动退桌；模块关闭则停用本机运行，下次可从主持存档恢复。
- LOCAL 消息同样受到宿主大小限制。根复核捕获了自然对局中超过 16 KB 的视图，改为卡牌 ID 加有界分片，保留全部公开记录、弃牌及私密选择；窗口实例和页面 nonce 排除旧页面，发送端只保留最新待发视图。界面大小调整保留原 iframe，不丢当前选牌。

规则书少数未定义或异常的状态会明确暂停并保留状态；此版本没有自动采用桌规或任意改牌功能。具体六类原因及所参考页码见 `docs/research/three-dragon-rules-coverage.md`。主持人仍是可信发牌者，能够访问自己浏览器中的完整牌局；这不是防主持作弊或服务端托管方案。

主代理从固定树 `e79c842ebdd00486fda7e536184edfcbfb9a6800` 导出隔离目录，重新安装锁定依赖，独立通过以下检查：

| 验证 | 结果 |
| --- | --- |
| stable / dev 构建、TypeScript | 均通过 |
| `three-dragon-rules-selftest.mjs --mutations` | 20851 条断言、24 整局通过，18 个变异被拒绝；牌与金币守恒及 40 类牌的能力分支 |
| `three-dragon-controller-selftest.mjs` | 12 组实际控制器与原生加密多实例回归，含错序、身份、失档、持久化失败、丢回执、重连与清理 |
| `three-dragon-controller-ui.mjs` | 实际 Edge 点击 → UI / 控制器 / 规则 / Web Crypto / IndexedDB，创建、加入、发牌、下注、关窗重开、失档取消/确认回大厅和再次开始通过 |
| `three-dragon-ui-selftest.mjs` | 256 条实际 DOM 检查；双语、合法选牌、焦点保留、390px 布局、分片回放和完整弃牌查看 |
| `runtime-selftest.mjs tools/three-dragon-private-selftest.entry.ts` | 7 组原生加密检查，含篡改、重放、分片、乱序、队列界限和失效会话 |
| `runtime-selftest.mjs tools/three-dragon-local-view-selftest.entry.ts` | 1198 个自然步骤，捕获最大完整视图 18162 字节；每片小于 12000 字节且完整还原，覆盖表情/转义、旧窗口与过期重试 |
| `three-dragon-storage-selftest.mjs`、`three-dragon-shell-selftest.mjs` | 实际 IDB 重开、并发事务和完整恢复；懒加载、关闭不离席、过期窗口、尺寸变化与迟到打开清理通过 |
| `suite-wiring-ui-review.mjs` | 24 个设置视图、12 个工具栏宽度组合，双角色入口、开关与窄栏命中通过 |

主代理查看了窄屏双语牌桌及双玩家不同手牌的实际浏览器截图。日志及截图保留于本机 `_audit/2026-09-08/phase5-validation`。稳定构建启动 gzip **139.7 kB / 43 chunks**，全部 JS gzip **466.6 kB**；控制器的约 16.4 kB gzip 按需加载。上述房间传输边界由测试端口模拟，不等于实际 Owlbear 多人、公网丢包、手机、长局和玩家体验验收；源码更新仍未部署线上。

## 第六批：角色卡英文显示与编辑目标一致性

角色卡信息页和全屏页补充属性、技能、职业/种族、武器属性与精通、伤害类型、固定单位、语言/工具等英文呈现。提供的独立英文名称和说明优先显示；未知自制名称、背景故事及作者正文保持原文，不把界面语言写回角色数据。主代理补充修正了仅有英文说明、没有基础 description 字段的专长被隐藏的问题。

- 信息入口按卡片 visibility / owner_ids 校验，GM 创建并分配给玩家的锁定单位可打开。固定窗口也响应权限撤销、删除、重新绑定、角色变化和场景关闭。同卡不同 token 更新真实编辑目标；延迟选择、窗口开关、初始读取和 READY 回放均排除旧目标。入口对 120 次单纯位置/HP 事件没有额外读取或开窗消息。
- 共享 HP 与资源编辑保留正在输入的表达式、空白数值和焦点，切语言不重建编辑器。实际 SDK 写入回调再次核对目标、场景、角色和组件生命周期；取消拖动不写入，资源拖动只在释放时保存，成功保存后才发布变化提示。并发字段、旧资源修复标记和旧血条插件的附加字段保留。
- 资源编辑器的本机连接和会话校验排除外部/旧窗口命令；关闭 iframe 的同步会话标记可阻止迟到保存。根复核先复现再修正两个遗漏：场景就绪前错误开放编辑，以及同源多个房间的编辑器互相覆盖会话标记。初始场景读取与角色事件现在分别计代次，各编辑器标记独立。
- 全屏角色卡加载有请求代次、取消和 12 秒截止。远端更新遇到本机草稿先显示通知；明确刷新可取消，读取失败保留原编辑区。页面关闭清理订阅，自己发送的更新不再重复读取。
- 全屏法术选择和缺失正文按已启用资料库、语言、来源黑名单与版本读取；首次权威设置到达前不请求默认库。显式 PHB 引用不会被 2024 筛选偷换为 XPHB；没有来源且同名多版本的旧条目需要选择。成功缓存有容量/期限，失败可同页重试，换配置撤旧请求和内容。完整结构正文、升环和施法机械字段只读呈现。

固定产品/测试树为 `2138b9dc4b6393db686f01f3d063c5c999e2775b`，主代理在隔离导出中安装锁定的 95 个依赖并独立验证：

| 验证 | 结果 |
| --- | --- |
| stable / dev 构建、TypeScript | 均通过 |
| `cc-info-dom-selftest.mjs` | 30 条信息页 DOM / 延迟读取检查通过；共享编辑器在此测试中以测试替身隔离 |
| `cc-entry-selftest.mjs --mutants` | 7 组实际入口检查通过，7 个变异被拒绝；根复核新增旧血条附加字段保留检查 |
| `cc-fullscreen-dom-selftest.mjs`、`cc-fullscreen-mutations.mjs` | 40 条实际 Edge DOM / 内容加载检查通过，3 个变异被拒绝 |
| `resource-ui-selftest.mjs --mutations` | 实际共享编辑器与资源编辑页 23 项通过，13 个变异被拒绝；包含两页共享浏览器存储和延迟真实写入回调 |
| `cc-info-integrated-selftest.mjs` | 根代理向上述隔离目录加入两份纯测试文件后，实际信息页 + HP + 资源 + i18n + Immer 组合 45 项通过；双语 320px、同卡换 token、输入焦点、旧读、同角色撤销 owner_ids、场景关闭和延迟写入 |

本批日志及界面图保留于本机 `_audit/2026-09-08/phase6-validation`。稳定构建启动 gzip **142.2 kB / 45 chunks**，全部 JS gzip **481.7 kB**，不能据此声称加载体积下降。主代理已查看全屏术语和真实组件组合的双语属性/资源截图；组合检查未发现装配缺陷。根新增专长测试最初误把默认展开的条目点击折叠，纠正测试操作后通过，未改变产品默认展开行为。

这批不等于全内容英文本地化完成：真正英文 XLSX、部分内置职业/子职/专长正文、其余页面和真实 Excel/WPS 导入验收仍未完成。资料库中仅有 _copy 而没有正文的特殊法术不在本批解析范围；找不到译名或来源时明确显示不可用。宿主传输由测试端点模拟，尚无实际 Owlbear 多人、缩放或公网性能验收。这里的 HP 编辑可靠性与用户新增的画布血条缩放错位属于不同路径。

## 第七批：传送门图片库与默认恢复

传送门编辑页新增当前图案预览、从自己的 Owlbear 原生图片库选择，以及恢复默认图片。图片直接保存到已有传送门，不新建插件或额外图片服务；按原显示范围的最长边适配并保留素材比例。只更新 image/grid/scale，保留位置、旋转、名称、链接、触发半径、独立转场、锁定及隐藏设置。

根复核同时修正三个实际边界：旧迁移原本会把任何自选 URL 改回默认，现在只识别已知套件默认图标并在写入时重查；初次读取失败有同页重新加载入口；原生图库的 MP4/WebM 动画图片也可选择，编辑页的视频小预览不自动播放，关闭时卸载媒体。真实场景播放仍交给 Owlbear 原生 Image。原生图库接口依据 [官方 Assets](https://docs.owlbear.rodeo/extensions/apis/assets/#downloadimages) 和 [ImageContent](https://docs.owlbear.rodeo/extensions/reference/items/image/#imagecontent) 核验。

固定产品/测试树 `8999cc2f149590150fe95f9d0586e11cdb5a9076` 在隔离目录重新安装锁定依赖，主代理独立通过 stable/dev、TypeScript、图片编辑 27 项实际 Edge / Immer 检查及 3 个变异；原转场逻辑仍为 19/19，双语转场/传送门 UI 为 9/9。原 UI 测试边界缺少新控件所用的 room、图片与 items 订阅，且未注入构建资源路径；补齐测试替身后回归通过，产品没有为测试移除控件。最终比固定树仅新增/调整两份原转场测试文件及本记录。

日志、实际预览与窄窗截图保留于 `_audit/2026-09-08/phase7-validation`。启动 gzip **142.7 kB / 45 chunks**，全部 JS gzip **483.7 kB**。图片读取/选择中遇到关窗、降权、切场景或删除传送门会撤销迟到操作。尚无真实 Owlbear 图库窗口、资产加载/动画、玩家同步和触发效果验收；本批模拟原生图库返回值，不把浏览器内测试写成宿主已验证。

## 后续顺序

用户追加要求已纳入持续目标：三龙牌改成更沉浸的全屏牌桌，包含底部手牌、悬浮放大、其他玩家牌区和公共牌区；传送门可更换图片、从自己的 Owlbear 图片库选取，并恢复默认图片；修复血条在缩放时异常错位，再评估低成本的血条材质及随血量/伤害变化的效果。用户明确说明血条错位此前十余轮修复失败，因此须从宿主坐标、缩放和渲染方式确认根因，不能凭模拟坐标检查宣布解决。

1. 角色卡及共享资源编辑验收、剩余界面本地化、真正英文角色卡模板下载。
2. 三龙牌全屏牌桌与传送门图片选择；沿用当前规则、私密手牌和弹窗关闭保留后台的约束。
3. 血条缩放错位根因与实际渲染验证；位置稳定后再评估装饰及程度效果，不以持续逐帧网络写入维持显示。
4. 玩家鼠标指示与跟随可行性原型；以公开 SDK 能力和真实地图操作为验收边界。
5. 汇总实际房间验收入口与剩余限制，继续简化界面和检查运行成本。
6. 历史迷雾研究放最后，沿用不复制未获许可代码或素材的边界。

GitHub 源码更新已获用户授权；每批更新对应 PR。安装中的 stable/dev 地址是否部署需按版本核对与验收记录说明，不能把源码更新写成线上已发布。
