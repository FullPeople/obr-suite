# Full Suite-dev 1.0.177 · 已部署

状态：本地实现与回归阶段。尚未声明已部署，最终完整测试、构建和线上文件校验由发布负责人追加。

## 交付范围

本轮覆盖用户提出的 18 项修订：固定连接状态栏；角色切换和选择响应；独立横向角色栏；数值资源失焦／回车提交；公共仓库锁；设置本地首屏及顶部鸣谢；玩家规则概述与个人来源显示；子职顺序正文与目录；场景常驻骰子记录；窄屏可读排版；自定义物品价格与重量；紧凑赠予落位与库存回执；网络同步与恢复；移除二级骰子旧 ×；黑夜模式；头像确认删除；中文站三方资料渠道；旧版角色文件适配。

Web 源码 docs/WORKBENCH-177.md 逐项描述交互。本轮低于 560 CSS 像素的卡片区域改为纵向重排，覆盖旧“所有宽度都缩放 A4”约定；宽屏、打印与 PNG 继续使用 A4。黑夜模式和来源显示方式为个人偏好，不修改房间规则。

## 同步与读取

- 角色文档、metadata 映射、库存账本沿用既有存储。文档发送变化字段，较大的消息使用压缩；服务端 patches.mjs 应与 server.mjs 一同部署。
- 活跃直接窗口优先，失活后允许降级中继；心跳不等待卡片、库存或资料库读取。对回执保留请求身份，执行中的重复请求复用同一操作，不重复投骰或切换开关。
- 队列中的过期请求在执行前终止。请求超时不能证明未提交，因此不盲目重放修改。迟到终态回执可解决本地未确认状态和库存草稿。
- 后台角色校准不能把批量遍历开始时的旧棋子快照应用到后来读取的新文档；逐卡临近读取，并在跨异步步骤写入前核对观察值。
- 旧角色保留文件位置与目录，切单卡不等全簿。怪物详情通过 data.ts 的 loadMonsterBySlug 复用原 detail-data.ts 单源／继承／语言策略，独立 2 并发请求通道不受全库 6 槽下载阻塞。
- 设置的外观结构和本机颜色先渲染，共享开关要等真实 SDK 变量；初始化一轮并行读取，不能把本机默认值写回房间。

## 已记录的验证和边界

设置／夜间／鸣谢 18 项浏览器检查、单怪物定向读取 10 项与夜间 Wiki 对比度 12 项专项检查已通过。Web TypeScript 与 78 项单元检查通过。新的网络压力与传输检查工具为 tools/workbench-177-network-selftest.mjs、tools/workbench-177-transport-selftest.mjs；最终执行数量和结果由发布负责人追加。

测试采用真实模块配合 SDK／网络边界模拟，不代表真实 Owlbear 多人房间、跨设备或实体平板验收。稳定版 manifest 不变；仅开发版升为 1.0.177-dev。上线后需同步刷新枭熊后台和工坊页面，避免继续运行旧代码。部署收据与公网校验尚待追加。

整包首次回归额外发现旧卡缺失 identity.size 被 UI 默认值替换导致的误冲突，已修复 delta 递归参数的显式缺失语义；传输专项 11 项通过。未知结果与后续未发送的排队动作区分处理，恢复只解锁新的用户操作，不重放旧操作。

## 最终构建与联动检查

完整发布构建通过。对该构建运行 `tools/workbench-177-selftest.mjs`，68 项联动检查全部通过、29 次实际模拟角色文件保存。覆盖旧卡首次添加种族、状态添加撤销重做不复活、仓库转移及撤销、资源连续点击、通知音效、权限和隐私、三档窄屏、Wiki 拖拽切页、怪物与人物布局。

另有 18 项慢网／宿主刷新专项、12 项压缩／增量语义专项、15 项双宿主回归通过；网页本轮 27 个浏览器案例（12 角色交互、3 响应式、4 资料、5 既有交互、3 未确认结果恢复）分组通过，78 项单元检查通过。真实多人房间和实体平板仍未实测。


存储收尾：`workbench-document-177-selftest.mjs` 8 项专项及原共享文档回归通过，包含真实 Windows 短暂文件占用、500 次写入配合 4,000 次读取、CAS 冲突、永久权限错误与磁盘满故障注入。Windows 仅重试同一临时文件的原子替换（最多 775ms 退避），不重放业务操作；Linux 路径保持一次原子替换。历史联动曾有一次 HTTP 500，但当时没有底层 errno，不能将它确定归因为已复现的文件占用；现在增加存储阶段与错误代码日志供追查。

## 上线回执

2026-09-23 已部署 `1.0.177-dev`。服务器 597 个发布文件与构建哈希一致；稳定版 335 个文件完全未动，仍为 1.3.5。外部 HTTPS GET 验证开发 manifest 为 1.0.177-dev；237 个 HTML/JS/CSS/图标/作者素材与两份源码包经过服务器 HTTPS 虚拟主机回环逐文件校验（保留 Host、URL 和证书校验，不将此描述为跨公网逐文件检查）。

新中继的注册、收发、宿主隔离、删除路径校验与共享文档持久 CAS 通过，独立探测数据已删除。开发 relay 的 Nginx 读取超时改为 90 秒；旧版网页和中继保留可回滚备份。

安装地址：https://obr.dnd.center/suite-dev/manifest-dev.json 。使用者需刷新枭熊与工坊页面。真实多人房间、跨设备和实体平板尚未实测。受控模拟预览已恢复：http://127.0.0.1:5197/preview 。

服务器回执已下载到 Suite `workbench-test-output/deployment-1.0.177-dev.json` 与 `public-1.0.177-dev.json`。源码包对应已验证发布代码；本节为部署后记录。
