# Full Suite-dev 1.0.180 发布候选

2026-09-24。用户已要求部署；本文件随候选源码打包，正式收据在发布完成后追加。

本次合并此前尚未上线的 179 功能／同步修复与自适应卡片特效修订。线上基线为 1.0.178-dev；稳定版为 1.3.5，不在本次发布范围。

## 内容

- 179 的资源流式布局、法术学习／预备交互、二层转移菜单、DM BBCode 笔记、默认棋子头像、场景通知与骰子记录拖动。见 WORKBENCH_SCENE_179.md 和网页仓库 docs/WORKBENCH-179.md。
- 当前卡远端刷新按卡独立调度，取消被新失效覆盖的旧读取，并在持久提交时立即发出最小更新通知。见 WORKBENCH_LIVE_179.md。
- 拖拽接收整张卡时保持条目尺寸，只向真实小格或气泡变形。总览、窄屏和怪物两页使用独立实测尺寸图形与内部框物理；原宽屏人物 A4 五页保持。见网页仓库 docs/ADAPTIVE-CARD-EFFECTS.md。

## 发布前证据

179 已完成 99 单元、89 整包联动及按卡缓存、事务、场景通知和重连专项。随后自适应修订完成 20 项定向浏览器检查、类型检查和网页生产构建。两阶段本地结果均已记录，不重复宣称真实多人房间验收。

保留已记录的单次状态添加超过 5 秒延迟：随后复测通过，但未取得足够证据解释该次偶发。笔记私有地址绑定当前 DM 浏览器，尚不支持自动跨设备迁移。

## 发布边界

使用 tools/build-workbench-dev.mjs 完整生成网页、宿主、嵌入面板及两份源码包。部署使用 expected=1.0.178-dev 和带校验清单的候选产物，保留可回滚旧目录。中继三个源码文件与当前线上逐字一致，部署脚本仅在发生变化时重启。

完成后核对服务器文件、HTTPS 虚拟主机返回、外部版本、中继通信／权限／CAS，并清理本次独立探测文档。稳定版文件需保持一致。公开源码包不含用户角色文件或上游私有数据快照。

## 发布完成

2026-09-24，1.0.180-dev 已部署。

- 发布前完整 Web、Suite、嵌入设置／音乐／三龙牌／骰子构建通过；归档输入检查覆盖两份源码包（Web 286 项、Suite 883 项）。
- 597 个服务器文件匹配构建校验清单；237 个页面／脚本／样式／图片／源码包经服务器 HTTPS 虚拟主机回环核对。另从外部 HTTPS 获取 dev manifest，返回 200，版本为 1.0.180-dev。
- 中继注册、消息送达、宿主权限隔离、删除路径校验、持久文档 CAS 检查通过；本次独立探测文档已删除。中继源码未变化，没有重启服务。
- 稳定版 335 个文件保持一致，版本仍为 1.3.5。回滚目录：`/var/www/obr-plugins/suite-dev-before-workbench-1.0.180`。
- 收据：`workbench-test-output/deployment-1.0.180-dev.json` 与 `workbench-test-output/public-1.0.180-dev.json`。

上述是发布与协议检查，非登录真实房间的多人操作验收。发布包源码保留构建时的候选文档快照，本节为随后追加的本地正式收据记录。
