# 2026-09-27 · 197 已发布，公网核验通过

国内单机 `standalone-1.0.197`、新版 Suite `1.0.197-dev`、旧版 Suite `1.3.6` 已上线，公告 `0.1.6`；GitHub Pages 完整门禁和部署通过。实际源码 Web `cd31b0a` / Suite `8996ff6`。服务器 1054 文件校验、公网 195 文件抽查与关键页面流程通过。以 [最终发布回执](RELEASE-197-RESULT.md) 为准，下方同日“候选/未发布”段落属于历史记录。

本次追加修复见 [RELEASE-197.md](RELEASE-197.md)，原始清单及图片共 33 项见 [白话对照](FEEDBACK-PLAIN-20260927.md)。多人视野、投骰按钮缺失、偶发放怪、实际房间性能等仍待验证；突然掉线自动接任尚未完成。

继续开发使用 `F:/CodexWork/2026-09-27/feedback/{web,suite}`。`D:/Desktop/DND-card-web` 与 `U:/枭熊插件/obr-suite` 的历史混合源码没有被覆盖，不能从这两个脏主目录直接发布。

> 最新追加与发布授权见 [RELEASE-197.md](RELEASE-197.md)。下文保留上一轮实现和验证过程，最终上线状态由部署回执确认。

# 2026-09-27 反馈修复：Suite 与旧插件

本轮在隔离工作树 `F:/CodexWork/2026-09-27/feedback/suite` 开发，基线为已发布 195 对应的 `66ca635`。Web 配套源为 `F:/CodexWork/2026-09-27/feedback/web`。未部署、未提交、未推送；没有修改有历史脏改动的 U 盘主工作树。本文的「通过」只指下述具体验证，不能代替真实枭熊多人房间验收。

## 实现

| 反馈 | 本轮行为与范围 |
| --- | --- |
| 总览、切卡与同步性能 | 场景棋子的纯位置/缩放/旋转事件不再触发工作台目录重算、快照广播和文档巡检；仍更新场景观测数据。目录按观测版本和文档缓存版本复用。当前卡变更判定不再反复 JSON 序列化含图片的大文档。文档广播与场景投影仍即时失效缓存，漏通知的条件 GET 兜底从 3 秒改为 2 分钟。 |
| 导出未选角色、DM 审卡 | 新增只读 `readCard` RPC，按现有可见权限读文档，不要求写权限，不携带 snapshot，不切换当前角色，也不触发写队列/额外目录刷新。 |
| 旧角色卡改用新 JSON | 旧面板、拖放、粘贴和逐卡刷新接受 JSON；通过 Web 的验证与导出桥归一成同时保留原生身份和旧宿主字段的数据。禁用 XLSX 选择/导入，制卡链接转向角色卡网站。上传与刷新沿用现有服务器 JSON 路由；无需修改服务端。 |
| 旧角色卡展示 | 打包 Web 五页只读阅读器，不启动 App/Wiki/角色编辑或本地角色仓库；角色正文来自上传的 JSON。旧 `cc-fullscreen.html` 收藏链接转到新阅读器，示例入口也复用新阅读器。 |
| 怪物放置偶发失败 | 图片探测 6.5 秒超时，失败重试一次；只缓存成功，合并同时探测，下次放置可重新恢复。格式根据 URL/data MIME 识别，失败使用同站原创建占位 SVG。共享怪物数据写入失败会停止创建棋子并保留重试能力，避免制造没有资料的怪物棋子。新旧宿主共用同一 spawn 模块。 |
| 自动归属光源共享 | 共享开启时，可见的自动归属光源可进入共享池，包括 DM 创建的棋子与未绑定棋子的光。显式仅 DM、空所属人、隐藏附着链、DM 私有卡继续受限；个人视野仍按卡所属人/棋子创建者计算。 |
| 玩家可见但不可操作的门 | 新增「门（仅 DM 开关）」工具（Shift+O）。门可见、开关状态正常参与视野计算，玩家不获得操作控件，也不能通过玩家开门命令操作。 |
| 没有卡/图鉴的血量组件 | 只有 HP 组件开关、尚无数据的棋子显示 0/0 待填写占位，且可在工作台编辑生命数据；不伪造实际生命上限。 |
| HP 算式 | 宿主支持安全数字、括号与 `+ - * /`；开头运算符表示相对当前值，`=`表示绝对式，结果按既有整数生命值向下取整。拒绝非数字代码、除零、溢出、过长/过深输入，不使用 eval。 |
| 骰子历史原式 | 骰盘和快速投掷广播保存 expression，骰盘历史与浮动历史显示完整原式；`max(1d20,20)`等包裹不再仅留下结果。正文 HTML 转义。 |
| 展示物品排版 | 来源等信息移至标题下副行，正文滚动区扩大；可分享详情白名单补充武器/护甲属性、职业初始熟练与装备、法术高环等字段。 |
| 旧公告 | 移除金色强调盒和外围卡片装饰，调整为中性标题/正文；使用自创角色与规则夹具截取真实新版工作台界面，新增大幅特色图。旧设置内的冗长 Owner 指南移交新版公告，设置保留简短入口提示。 |
| 三龙旁观全能 | 无座位的主持/获准 DM 使用公共视图为基础生成独立私有检查视图，不再因空座位 ID 抛错。公共视图仍无完整手牌。 |
| 三龙运行成本 | 静止场景不再因手牌的环境动画每帧重绘阴影；布局/动作/揭示/落牌变化时才更新。没有以此宣称实际设备 FPS 达标。 |
| 三龙创建者离开 | 增加主动「移交主持后离开」。选择在线继任者，完整状态通过既有 AES 私有分片交付，继任者先校验并以 CAS 存储，确认就绪后才公开更改主持身份。活跃局座位、手牌和待处理队列不删除。详见同轮三龙移交报告。突然断网或直接关闭浏览器不等于完成主动移交。 |

投骰按钮本轮未复现「只对部分玩家消失」。宿主默认开启，Web 导航不按 GM/PLAYER 隐藏，未联网或房间关闭骰子时禁用。该项记录为目前已具备、特定房间/旧包差异仍需实际环境复核，不冒记为本轮修复。

## 性能：同夹具 195 与本轮对照

探针：`tools/workbench-perf196-selftest.mjs`。实际安装的 Owlbear SDK 经过真实浏览器 iframe 与 parent postMessage 边界；模拟 HTTP 后端、3 张自创卡，30 次纯位置更新，缓存人为老化 5 秒，随后交替选卡 12 次。旧源码及旧 observation 一并从同一 195 工作树读取，避免只换入口留下新缓存。

| 测量 | 195 | 本轮 |
| --- | ---: | ---: |
| 30 次位置更新后的目录重建次数 | 210 | 0 |
| 对应 SDK RPC（含生命周期噪声） | 4 | 2 |
| 位置更新卡 JSON 正文下载 | 0 | 0 |
| 缓存老化 5 秒后的周期 GET（含 304） | 1 | 0 |
| 12 次暖缓存切卡的卡 JSON 下载 | 0 | 0 |
| 最终源码该次夹具 12 次切卡总耗时 | 73 ms | 75 ms |

根因证据是目录重建与不必要 GET 的减少。**旧版暖缓存切卡已经不下载 JSON，不能声称此前每切一张卡都全量下载。** 早期新源码为 57 ms，缓存审查后最终重跑为 75 ms，旧版为 73 ms；单次小夹具耗时有调度噪声，没有证据宣称本轮降低了暖切卡延迟，更不能当作用户真实卡顿消失。目录/文档事件仍有全量数据传输，本轮没有伪称所有路径都改成最小字段增量协议。

重现命令（PowerShell，Suite 工作树）：

```powershell
$env:PROFILE_SOURCE='F:/CodexWork/2026-09-20/w-xu/fixes194/suite/src/workbench/background.ts'
$env:WORKBENCH_PERF196_OUT='F:/CodexWork/2026-09-27/feedback/suite-results/perf195'
node tools/workbench-perf196-selftest.mjs
Remove-Item Env:PROFILE_SOURCE
Remove-Item Env:WORKBENCH_PERF196_OUT
node tools/workbench-perf196-selftest.mjs
```

原始结果：`../suite-results/perf195/perf.json` 与 `../suite-results/perf196/perf.json`。旧 background SHA-256：`8fdb4fac74c67f7b16b5603521c4fbaeeedc29030c7fb445699cfe4b44370ab3`；最终新 background SHA-256：`2b60d6a1c79e019c53300dbedf954da66515d1f5da65787aaf989ee013d88e1c`。独立审查另修正缓存命中时保留最新棋子对象、自定义怪物存档读取使目录失效、目录优先使用怪物覆盖数据，避免性能优化造成新数据迟滞。

## 验证与构建

- `node tools/feedback196-selftest.mjs`：27 项纯逻辑检查通过，覆盖安全算式、光源共享/明确禁止/隐藏、仅 DM 门序列化、无座位全能与公共投影隔离。
- `WORKBENCH_RESOURCE194_OUT=../suite-results/host196 node tools/workbench-resource-194-selftest.mjs`：14 组通过。真实 SDK/iframe，模拟双写入方与 HTTP 后端；包含原资源并发/冲突回归、新只读 readCard 不切卡不写入，以及私有卡不可越权读取。
- `node tools/three-dragon-controller-selftest.mjs`：15 组现有 controller/native-crypto 回归通过。
- `node tools/three-dragon-handover-selftest.mjs`：6 组真实 controller/native-crypto 主动移交验证通过，包括大于 64 KiB 存档分片、远端未入座 GM 全能与降权。网络/房间为夹具，不是真实房间。详情见 [三龙移交记录](THREE_DRAGON_HANDOVER_20260927.md)。
- `node tools/workbench-cache-review196-selftest.mjs`：独立审查新增 9 项通过。纯移动/未知 metadata 后读最新对象、目录不重建；自定义怪物初读/刷新及时进入目录，优先于基础资料；重复读复用；锁定与角色降权仍拒绝读。
- Suite `tsc --noEmit` 与本机 Vite 生产构建通过。Web Vite 与阅读器 bridge 打包通过。未调用部署脚本。
- Web `node node_modules/@playwright/test/cli.js test -c playwright.suite196.config.ts`：原创卡+原创 Wiki 来源的公告截图通过，已目视检查 `public/workbench-preview.png`。
- `tools/feedback196-browser-selftest.mjs`：完整 10 组生产浏览器通过，结果 `../suite-results/browser196/results.json`。覆盖图片瞬断重试/成功缓存、失败不缓存恢复、挂起请求 13,217 ms 后占位、放置数据失败中止/下一次成功、原式广播与历史、公告截图、XLSX 请求前拒绝、JSON 上传后五页只读且零 Wiki 请求、JSON 刷新保持卡 ID、窄宽可见。另 `LEGACY_ONLY=1` 重跑 5 组保存为 `legacy-results.json`，多覆盖旧收藏链接重定向，未覆盖掉完整回执。所有场景为模拟房间/API，无浏览器脚本错误。
- 生产嵌套测试发现阅读器 `.paper` 隐藏：实际根因是 `lazy(()=>viewer?import(Viewer):import(App))` 使 Vite 仅预载 App CSS，Viewer 专属 CSS 没有请求。Web 改为各自独立 lazy import 后重新构建，完整/窄宽/切五页回归通过；不是简单堆 `!important` 掩盖媒体查询。

阅读器打包要求显式选择已审查 Web 工作树，不猜测 D 盘副本：

```powershell
$env:DND_CARD_WEB_ROOT='F:/CodexWork/2026-09-27/feedback/web'
node node_modules/vite/bin/vite.js build --outDir dist-feedback
node tools/build-legacy-card-viewer.mjs --web-built --out-dir dist-feedback
node tools/feedback196-browser-selftest.mjs
```

`--web-built` 要求先在上述 Web 工作树完成 Vite build。常规 `npm run build` 会构建配套 Web；`npm run build:workbench-dev` 会同时打包新工作台、旧只读阅读器及源文件归档。输出均留在本机。

三龙独立 `npm run build:three-dragon` 的 TypeScript 阶段通过，但 195 隔离源码缺少 `extensions/three-dragon-ante/{background,index,launcher,practice}.html`，其独立 Vite 构建失败。工作台采用根目录 `three-dragon-ante.html` 与 `tools/build-workbench-panels.mjs`，须分别记录其构建回执，不能将独立插件包记为构建通过。

最终执行 `DND_CARD_WEB_ROOT=F:/CodexWork/2026-09-27/feedback/web node tools/build-workbench-dev.mjs` 完整通过，包含两库 TypeScript、Web/Suite Vite、骰盘、设置/音乐/嵌入式三龙、旧五页阅读器桥和源码 ZIP。输出 `dist-workbench-dev`；日志确认 `Embedded table and Music Studio built` 与 `Legacy native card viewer ready`。构建存在既有动态/静态 import 混用及 Web App 530 KB chunk 警告，未以此声称冷加载性能达标。

截图：`public/workbench-preview.png` 为公告实际采用的自创卡+Wiki图；`../suite-results/browser196/legacy-announcement.png`、`legacy-viewer.png`、`legacy-viewer-narrow.png` 为生产公告/旧面板阅读器验证截图。均已目视检查，无私人角色或新增上游正文快照。

## 未覆盖的实际环境

需要真实枭熊房间验证：两位玩家/DM 的事件传播、跨标签页大角色与全量 Wiki 冷/暖切换、光源私有/共享的视觉结果、玩家仅 DM 门权限、无卡 HP 气泡实际位置与编辑、远程图床/Owlbear 图片代理真实故障、不同浏览器的骰子入口、三龙主持主动离开全过程与继任者刷新恢复。

怪物远程图片失败的原始报错与上游服务状态未知。本轮验证的是故障恢复边界，不能断言完全找到了用户所有偶发加载错误的唯一原因。三龙非正常断网/强制关闭时，尚无预先确认的完整私有备份，不能自动凭公开场面重建隐藏牌；保留该限制，未以公开广播全手牌换取恢复。
