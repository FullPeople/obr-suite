# 185 信息记录区

资源、物品、状态与共享词条统一进入右下角骰子记录区；普通信息 8 秒、共享词条 20 秒，显示倒计时并自动移除；骰子记录保留。点击信息只跳转点击者的 Wiki。设置 / 编辑对话框保持原用途。

面板复用同一 iframe，支持拖动、关闭、布局重定位、过期恢复及去重。关闭手势之后到达的新消息会重新打开面板。沿用通知确认、重试与锁定角色的隐私裁剪；不等待广播确认或音效完成再提交资源操作。分享只发送白名单词条快照，不发送人物文档。

按用户要求不发布本轮屏幕适配，骰记缩放下限也恢复 184 行为。原窄屏功能与状态效果保留，新增 Buff 排除。中继代码和稳定版不在本次修改范围。

tools/workbench-activity-185-selftest.mjs 使用实际 SDK 与双浏览器参与者，但模拟 Owlbear 宿主 RPC。其结果不等同真实多人房间验收。发布结果见 WORKBENCH_RELEASE_185.md。
