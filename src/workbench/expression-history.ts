const key='workbench:dice-expression-history';
let index=-1,draft='';
function history():string[]{try{return JSON.parse(localStorage.getItem(key)||'[]').filter((v:unknown)=>typeof v==='string').slice(0,50);}catch{return [];}}
export function rememberExpression(value:string){const expression=String(value||'').trim();if(!expression)return;try{localStorage.setItem(key,JSON.stringify([expression,...history().filter(v=>v!==expression)].slice(0,50)));}catch{}index=-1;}
export function installExpressionHistory(){const input=document.getElementById('exprInput') as HTMLInputElement|null;if(!input)return;input.addEventListener('keydown',event=>{if(!['ArrowUp','ArrowDown'].includes(event.key)||event.altKey||event.ctrlKey||event.metaKey||event.shiftKey||event.isComposing)return;const rows=history();if(!rows.length)return;event.preventDefault();event.stopImmediatePropagation();if(index===-1)draft=input.value;index=event.key==='ArrowUp'?Math.min(rows.length-1,index+1):Math.max(-1,index-1);input.value=index===-1?draft:rows[index];input.dispatchEvent(new Event('input',{bubbles:true}));input.setSelectionRange(input.value.length,input.value.length);},true);input.addEventListener('input',event=>{if(event.isTrusted)index=-1;});}
